# Handoff — 2026-08-07

_Generated by the SessionEnd hook. Rewritten by the next session that
changes something; a read-only session leaves it alone, so the date
above is when work last happened, not when Claude last ran._

**Branch:** `main`

## Next action

Decide what to do about the 11 orphaned systemd units and 7 unwatched host units the new service-discovery agent found, using the exact commands in `GET /api/units/actions`.

_(Session 26 shipped the detector; acting on what it found is a separate,
deliberately manual decision — the agent never touches the machine.)_

## What Session 26 delivered

The unmonitored-unit detector, Tiers 1 and 2. Five new modules, two
migrations, two endpoints, 109 tests (suite 1276 → 1385). Full write-up in
[tasks.md](../roadmap/tasks.md) under Session 26 and in
[STATUS.md](../roadmap/STATUS.md).

- `sysadmin/services/units.py` — pure sweep and classifier
- `sysadmin/services/unit_recommendations.py` — pure advice + YAML snippets
- `sysadmin/agents/service_discovery.py` — Tier 1 agent, 6-hourly
- `sysadmin/models/unit_audit.py` + migrations 006, 007
- `sysadmin/routers/units.py` — `GET /api/units/status`, `/actions`

There is deliberately **no Tier 3**. Sessions 22 and 24 rank by
health-score points and reclaimable megabytes, both directly measurable;
nothing here makes two host units meaningfully "twice" one orphan, so
`UnitRecommendationInfo` carries no score field rather than inventing a
currency, and a weekly narrative over findings that change monthly would
be prose about nothing.

## The live result

44 units seen, 6 distro/template excluded, 38 scanned:

| Bucket | Count |
|--------|-------|
| monitored | 12 |
| timers folded into their oneshot service | 8 |
| **orphaned** | **11** |
| unmonitored | 0 |
| **host** | **7** |

Zero `unmonitored` findings is the headline — every live project's units
are already wired. The estate's actual debt is the other two rows.

## Pending ops actions

Three, in priority order. None is automated on purpose.

1. **7 host units have nothing watching them**, including
   `pgbackrest-backup.timer` — the estate's only database backup. A backup
   that stopped a month ago looks exactly like one that ran.
   `GET /api/units/actions?kind=host` has the config.yaml snippets.
2. **11 orphaned units** are installed and broken (`garmin-sync`,
   `ticktick-sync`, `ticktick-sync-db`, `offline-agents-dashboard`, five
   `personal-assistant-*`, two `personalassistant-*`). Confirm
   PersonalAssistant and the two MCP directories are genuinely gone, then
   use the `systemctl disable --now && rm` commands in `?kind=orphan`.
3. Carried forward: `sudo systemctl restart sysadmin.service` (needed for
   this session's config.yaml block to take effect and for the new agent to
   start on schedule), and `systemctl --user enable
   sportsanalyser-backend.service`.

## Known limitation

The name-prefix matcher is a strict prefix test, as specified. That is
right for `sportsanalyser-pipeline` → `SportsAnalyser` and correctly
refuses `deadlock-api-ingest` (no such project exists), but it also means
`venture-chat-large` and `venture-embed` do **not** match
`venture-assistant`, and land in `host` rather than `unmonitored`. The
advice is still correct — both are llama.cpp sidecars that want a
config.yaml `services:` entry, not a projects.yaml endpoint — so this is
noted rather than fixed. Loosening to a token-prefix test would gain those
two and risk false positives elsewhere.

## Next session candidates

- **Session 26b** — port-registry reconciliation. Decided this session:
  option **(b)**, move the allocation into `config.yaml` and render the
  guide's table from it. Reuses `units.py`'s parsed `exec_start` lines and
  its scope-aware unit identity.
- **Session 25** — service reliability scoring.
- **Session 27** — log aggregator tiers, take with SNAG-AGENT-002.
- **Session 29** — the one-thing endpoint (momentum track).
