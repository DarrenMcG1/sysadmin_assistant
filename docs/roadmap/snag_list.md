# Snag List

> **Quick Reference**: See [STATUS.md](STATUS.md) for overall project status
>
> **Related**: [tasks.md](tasks.md) | [ideas.md](ideas.md)
>
> **Last Updated**: 2026-08-17

---

## Open Issues

_**One closed and one opened on 2026-08-17 by Session 61, and the live parser reads 42 → 43** (measured either side of the edit by driving estate-manager's `count_open_snags` over this file, not by adding one). The rise of one for a sitting that closed as many as it opened is `SNAG-ROADMAP-002` yet again: the parser counted the opening and not the closure, because a fixed entry stays in place under "Open" with its marker in the header and the marker is prose to it.** `SNAG-AGENT-008` is **fully fixed** — its priority half landed and the entry's own statement of the trade-off turned out to be wrong in two of three clauses, settled by one `systemctl show`. `SNAG-LOG-003` opened in its place, named by the fix rather than found separately: the errors this daemon can finally see will arrive with a JSON document where the message should be. `SNAG-LOG-002` had its composition re-measured and it is 9 sources rather than 2. **Previously: two entries corrected and none opened or closed on 2026-08-17 by Session 60, and the live parser is unmoved at 42** (measured either side of the edit by driving estate-manager's `count_open_snags` over this file, not by assuming a correction cannot move it). `SNAG-AGENT-008` had its **volume half fixed** and three of its claims corrected — the ceiling is hit on the first read after a restart and at no other moment, the causes were a duplicated log line and a tray fan-out rather than "an access-log line per request nobody revisited", and the composition had never been measured before being asserted. `SNAG-LOG-002` had its **cause corrected**: the 118 truncated runs are kernel 103 against sysadmin-service 14, out of 10,064 runs, with 104 of them on a single day (**and that pair is 2 of 9 sources — re-measured 2026-08-17 by Session 61, see the entry**). So a P1 shrank by half and a P2 changed owner without either count moving, which is `SNAG-ROADMAP-002` from the other direction — the parser cannot see a partial fix any more than it can see a closure that stays in place. Previously: **four opened on 2026-08-17 by Session 59, none closed, and the live parser read 42** (measured either side of the edit by driving estate-manager's `count_open_snags` over this file, not by adding four to yesterday's number). One of the four — `SNAG-DB-004` — was **fixed in the same sitting** and stays here under "Open" for the write-up, which is `SNAG-ROADMAP-002` again: the parser cannot see a closure that stays in place. Two of the others were found by the fix rather than looked for (`SNAG-AGENT-008` by asking why nine nights of `ERROR` raised no alert; `SNAG-LOG-002` by the new endpoint returning none of the rows it was written to return), and one — `SNAG-LOG-001` — by reading a live payload instead of a fixture. Previously: **two closed and two opened on 2026-08-17 by Session 58**, a net of nothing. Closed: `SNAG-DOCS-001` (fixed) and `SNAG-AGENT-002` (**fixed on 2026-08-12 and closed on paper only**, the third documented item this month found already done). Opened: `SNAG-DOCS-002` and `SNAG-ESTATE-010`, both named by the fix rather than found separately. **The live parser went 36 → 38**, measured either side of the edit rather than reasoned about, and it is worth stating what that number means: the parser counted both openings and **neither closure**, because a fixed entry stays in place under "Open" with its marker in the header and the marker is prose to it. So the estate's board will publish a rise of two for a sitting that closed as many as it opened. That is `SNAG-ROADMAP-002` demonstrating itself on the sitting that fixed a documentation snag — and the entry is still filed here although the parser moved to estate-manager on 2026-08-13, which is the same drift one level up. Previously: **`SNAG-ESTATE-009` opened 2026-08-17 by Session 57**, which took the ports family's first live rows and left behind exactly what it could not reach: the six-hour sweep window. The live parser moves 35 → 36, measured by running estate-manager's own `count_open_snags` over this file before and after the edit rather than by adding one to the last number written down. Note that `GET :8400/api/projects/board` said **34** at the same moment, because a board row is a stored snapshot and the last commit had not been scanned yet — the same rule that says not to trust this preamble applies to the API that would correct it. Previously: **`SNAG-DB-002` and `SNAG-UNITS-005` both closed on 2026-08-17 as `SNAG-ESTATE-008` opened**, the last being the reason the first two stayed open on paper after being fixed on the box. Two closures and one opening is a net of one, and it is named rather than left to arithmetic because the interesting part is that **neither closure was performed by this repository** — the estate reindexed, the owner removed the units and restarted the daemon, and all three were discovered by measuring rather than by being told. The live parser is unmoved at 35 either way, which is its own comment on the taxonomy. (`SNAG-ESTATE-006` and `SNAG-ESTATE-007` were opened on 2026-08-16 by Session 54, both **delegated** to estate-manager.) `SNAG-ESTATE-003` was **fixed on 2026-08-16** and `SNAG-TRAY-007` opened the same day in its place, then **fixed the same day** by Session 55 with `SNAG-TRAY-008` opened in *its* place — the third link in a chain where each fix names what it does not cover, which is the intended behaviour rather than a treadmill; `SNAG-DOCS-001` was also opened on 2026-08-16, by reading the live route table instead of the document that describes it; `SNAG-DB-003` was fixed on 2026-08-16 and `SNAG-RELOAD-001` added **and fixed** on 2026-08-15), plus seven found and fixed the same day or since and left in
place for the write-up (`SNAG-SYSD-002` on 2026-08-08, `SNAG-DB-001` on
2026-08-10, `SNAG-CFG-001` on 2026-08-11, `SNAG-AGENT-003`/`-004` on
2026-08-12, and `SNAG-RELOAD-001` on 2026-08-15) — the entries listed therefore exceed the
entries outstanding, which is itself an instance of `SNAG-ROADMAP-002`.
**This used to claim `count_open_snags` reports 15, then 47. It reports
35** (measured 2026-08-17 by driving
`estate_service/projects/roadmap.py` against this file, which is where
that function has lived since the 2026-08-13 migration — this repository
no longer contains it, so the claim had outlived the code it named as
well as the count). The drop of 12 is **almost none of it this
document's**: one entry was added on 2026-08-16 and one closed, and the
parser has since been rewritten around `read_snags`, which returns a
detected format and an `is_open` flag per row rather than counting
bullets — so it now reads 37 entries and judges 35 of them open. A
measured number in a document goes stale when the code that measures it
moves, which is `SNAG-ESTATE-008` in miniature and the third time this
header has been wrong. The parser counts `- [P…]` lines under
**every** heading whose text contains "open", and this file has more than
one. Correcting it to a measured number rather than re-deriving the
taxonomy is deliberate — the taxonomy is `SNAG-ROADMAP-002`'s job — but a
header disagreeing with the live parser by 32 is precisely the failure
that entry describes. Both numbers were a +1 rather than an
audit when `SNAG-UNITS-002` and `SNAG-ESTATE-004` were opened on
2026-08-14; **`SNAG-UNITS-002` was fixed on 2026-08-15** and is left in
place for the write-up, so the count moves the other way without being
re-derived. `SNAG-ESTATE-001` stays open with its speaking half fixed and
its retirement-checklist half not.
`SNAG-AGENT-003` and `SNAG-AGENT-004` were both **fixed on 2026-08-12
(Session 41)** and both had their filed cause corrected in the process: 003's
two candidates were neither of them right, and 004's population was
understated by about twenty times. `SNAG-AGENT-005` was opened by that
session for the 547,891 log-aggregator rows it deliberately did not sweep up.
`SNAG-ESTATE-001` came out of **reading** those rows rather than counting
them: two retired PersonalAssistant units have restart-looped 52,178 times
since the 2026-08-04 reorganisation and stalled the kernel on 2026-08-08.
The service-discovery agent had already classified both as `orphaned`, with
the dead path and the reason in plain English, eight days earlier.
`SNAG-DB-002` and `SNAG-SYSD-003` were both found sideways by Session 39 while
looking at something else — a collation warning printed by `psql`, and a retired
`ollama.service` in the unit file being edited. Neither was the session's
subject, and both were recorded rather than fixed for reasons given in the
entries. **`SNAG-DB-002` closed on 2026-08-16 without this repository doing
anything**: its remedy half had been carried out by estate-manager on
2026-08-13, and the three days in which four documents here went on asking for
it are `SNAG-ESTATE-008`.
`SNAG-AGENT-004` was found on 2026-08-11 by looking at the live table to
confirm the Session 39 ladder had fired — 26,270 unresolved alerts for four
retired services, which is the project side's set-based-resolve defect
reappearing on the service side at sixteen times the scale.
`SNAG-PROJ-013` was found by Session 32 on 2026-08-11: the momentum endpoint
cannot see a session in a repository that never dates its handoff.
`SNAG-CFG-001` was found by Session 31, which needed to know which severity is
audible and found that the obvious knob is not the one; the fix turned out to
be a whole dead notification path rather than a stale key. The twelve
`SNAG-PROJ-*` entries from the 2026-08-07 project-organiser capability audit
were **all cleared by [Session 34](tasks.md) on 2026-08-10** and are archived
below with their fix dates. `SNAG-ROADMAP-003` — found 2026-08-08 while writing
the `.project.yaml` manifests — was **fixed on 2026-08-10 (Session 37)** along
with the SessionEnd hook that had been generating the stubs it preferred. The
`BRIEF`/`ROADMAP` entries were
found on 2026-08-07 by the consumer — Alfred now renders `briefing/preview`
daily and is building a page on `/api/projects/board`, so producer-side content
defects have a reader for the first time._

- [P3] SNAG-LOG-001: **one incident produces four recommendations, because systemd narrates a crash in four lines** (2026-08-17, opened by Session 59)
  - **Symptom, from the first live run of `GET /api/logs/actions`**: the 2026-08-12 mosquitto core dump yields **four** `new_signature` rows — `Failed to start Mosquitto MQTT Broker daemon.`, `Process N (mosquitto) of user N dumped core.`, `mosquitto.service: Failed with result 'core-dump'.` and `mosquitto.service: Main process exited, code=dumped, status=N/SEGV`. Each is a genuinely distinct fault signature and each is correct; together they are one crash, and 4 of the 13 rows on the page describe it
  - **Why it is not a dedup bug**: `log_signature.py` rule 1 exists precisely because a source-level key let the Bluetooth storm mask an RCU stall. Merging these four would need to know they are causally related, and nothing does — they share a unit and a minute, which is also true of four independent faults during a bad minute
  - **Why it is not simply a cap either**: the family already has the shape (`attention_max_rows`, `port_breach_max_rows` — collapse above N, take the loudest rung). A cap would hide the fourth row without saying the four were one thing, which is the roll-up defect `SNAG-ESTATE-001` spent a session removing: a count cannot name anything. The interesting fix is a *correlation* rule — same unit, same window, one of them a `Failed to start`/`Main process exited` line systemd emits about another — and that is a rule nobody has measured
  - **Cost is bounded**: this is a GET advice endpoint, not an alert family, so it produces no toasts. It costs the reader's attention on one page
  - **Found**: 2026-08-17 by Session 59, by reading the endpoint's first real output rather than its fixtures

- [P2] SNAG-LOG-002: **the noise recommendation has an empty population on this box — and the reason is the kernel, not this service** (2026-08-17, opened by Session 59; **cause corrected 2026-08-17 by Session 60**; ceiling half fixed by Session 62; **FIXED 2026-08-17 by Session 63**)
  - **Fixed 2026-08-17 by Session 63, and the two live rows are the falsification.** `log_trends._confidence` was `if coverage.runs_truncated > 0: return LOW` — binary, so one catch-up read pinned the whole report for fourteen days. It is now `truncated_fraction > TRUNCATION_LOW_FRACTION` (0.05) over the **instrumented** reads. Driven against the live database after the change: confidence `medium`, **25 recommendations including the 2 `noise` rows** the family was written to produce — both Bluetooth firmware signatures at 39,921 occurrences, exactly Tier 2's scoped example
  - **The handoff's stated mechanism was wrong, and measuring it is what made the change safe.** It named "the post-restart catch-up read, which `_resume_floor()` sizes by daemon downtime". `_resume_floor()` returns the newest stored `logged_at` **for that unit**, so it sizes the window by *how long since that source last stored a row* — which for a quiet source is days, not the two seconds a `systemctl restart` takes. That is why the 16 non-storm truncations each name **four or five sources at once**: measured, every one of them is the first `log_aggregator` poll after a restart, ~62 s after `Started SysAdmin…`, and a source logging one warning a week is read a week back on every restart
  - **Which is also why Session 62's ceiling fix reaches them, against the expectation that no ceiling could.** With `-p`, the 500-entry budget is spent on *storable* entries, and a week-long catch-up window on a quiet source holds about one. Live proof on the same box, same day, same restart shape: **13:17:05 restart → the 13:18:08 poll truncated 4 sources** (pre-`-p`); **14:10:58 restart → the 14:12:00 poll clean** (post-`-p`). So the population this gate was written for is smaller than either the entry or the handoff supposed — the gate is what stops the *history* of it suppressing the family for a fortnight
  - **The denominator was wrong and is fixed in the same change.** `_trend_coverage` counted the numerator over runs carrying `details['truncated_sources']` and the denominator over **every** run in the window. That field first appears 2026-08-12 17:31, so 10,724 of the window's 17,730 runs could not have reported truncation. Measured: **120 of 7,006 instrumented (1.71 %)**, not 120 of 17,730 (0.68 %) — 2.5x, self-correcting as those runs age out, which is precisely why leaving it was not an option: a number wrong today and right next week is one nobody re-checks
  - **Lowering the gate is not what happened, and the asymmetry is what makes a threshold legitimate at all.** A truncated read *drops* entries, so it can only make a count too **low** — a `noise` row argues a signature is loud, and a floor cannot be undercut by data that is missing. That is rule 4's own `NEW` asymmetry ("a gap can hide a fault, never invent one") one step further. What the threshold bounds is not the volume error but the chance a depressed *current* window moves a genuine `SURGED` signature into the noise-eligible `STEADY` band. Both live rows are `RETURNED` with `previous = 0`, so no ratio is computed for either and nothing is distorted
  - **`HIGH` is untouched**, deliberately: it still means nothing was lost and nothing was missed. Only the floor beneath it moved, so a report that loses a little is `MEDIUM` — which `_is_noise_candidate` already treats as good enough, since it only ever tested for `LOW`
  - **`TRUNCATION_LOW_FRACTION` is invented and says so**, on `RATIO_MIN_COUNT`'s terms, but with the numbers to hand: the fortnight reads 1.7 % and the storm's own day reads 104 of 1,434 (**7.3 %**), so 5 % puts a fortnight containing one bounded storm at `MEDIUM` and a window that is mostly storm at `LOW`
  - **`truncated_fraction` fails closed on not-knowing** — `schema_guard`'s posture, not `collation.py`'s. A caller reporting truncation with no `runs_instrumented` gets `1.0`, so every pre-existing test and every unmigrated caller keeps the binary behaviour. That is also why the guards had to be falsified: **all 1,984 tests passed on the first run after the change**, because the old fixtures set no denominator
  - **Falsified before being trusted.** Setting `TRUNCATION_LOW_FRACTION = 0.0` restores the binary rule *exactly* — it is the limit case, not a replacement — and breaks precisely the four new proportional tests and nothing else
  - **Half fixed 2026-08-17 by Session 62, and the fix named below was refuted before it was written.** Per-source confidence was driven against the live database through the real `_build_trend_report` → `recommend()`: it produces **zero** noise rows. The entire noise-eligible population on this box is two kernel signatures at 39,920 apiece, and kernel carries **103 of the 120** truncated runs, so per-source confidence gates the only candidates on the only heavily-truncating source. The eight sources it liberates have a loudest signature of **54**, against `NOISE_MIN_OCCURRENCES = 100` — under the gate across the whole 40-day retained history
  - **It would also have failed silently, on a seam this repository has already documented.** `agent_runs.details['truncated_sources']` names sources by their `services.yaml` **name** (`alfred`, `sports_analyser`); `log_entries.source` and `SignatureTrend.source` hold the **unit** (`alfred-backend.service`). `kernel` is the one string in both, so the obvious join reads all eight non-kernel sources as untruncated and kernel as truncated — wrong in both directions, and green. `_log_source_scopes` carries the same warning one function over
  - **The denominator was wrong too.** `details['truncated_sources']` first appears on the run at **2026-08-12 17:31**; 33,090 earlier runs have no such key. So it is 120 truncated of **6,974 instrumented** runs, not 119 of 10,063, and "kernel truncated in only one week" is an artefact of when the field landed rather than a fact about kernel — the July weeks at 87k/69k/116k Bluetooth lines predate it entirely
  - **What was actually wrong was that the ceiling counted the wrong lines, and that is fixed.** `read_journal` applied `severity_filter` in **Python, after** `-n 500` had already bounded the raw journalctl output. Measured across the storm (12:33–19:11): **203,042 raw kernel lines carrying 81,216 storable ones — 40 %**. Per minute, a median of **510 raw against a ceiling of 500**, so **208 of 210 storm minutes truncated**; the 100 instrumented storm minutes produced **103 truncated reads, one per poll**. Passing `-p` to journalctl makes the budget 100 % useful and stores an **identical multiset** (verified line by line), so the effective ceiling rose 2.5× with no change to `max_entries_per_read` and no change to what is stored
  - **This does not close the entry, and the remaining half is the binary flag rather than the per-source one.** Catch-up reads survive: `_resume_floor()` sets the window to how long the daemon was down, so one restart behind a backlog truncates however high the ceiling is. Since `_confidence` is `runs_truncated > 0` over a 14-day window, **one such read still pins the whole report `LOW` for fourteen days.** The candidate is therefore proportional-rather-than-binary confidence — which the entry already names below and already says should not be done to unblock a demo, so it wants its own sitting and an argument about honesty rather than about a number
  - **Re-measured 2026-08-17 by Session 61, and the composition is estate-wide rather than two services.** Session 60 named "kernel 103, sysadmin-service 14", which reads as a two-service problem and is 2 of **9** sources. Live over 7 days: **119 runs truncated of 10,063**, and per source `kernel 103, mosquitto 15, sysadmin-service 14, sports_analyser 13, venture-assistant 11, estate-manager-api 3, alfred 3, postgresql 1, alfred-frontend 1` — **164 source-truncations across 119 runs**, so some runs truncate several. Eight of the nine sources truncate occasionally
  - **This strengthens rather than weakens the per-source-confidence candidate.** A global binary flag is being tripped by nine different sources on their own schedules, so the report is `LOW` far more reliably than any one storm explains, and "wait for the 08-12 spike to leave the window" does not recover it. `details['truncated_sources']` already names the source per run; nothing aggregates it, and that aggregation is the whole fix
  - **Session 61's priority-half fix does not close this either**, and now for a measured reason rather than an inferred one: taking `sysadmin-service` to 0 leaves 105 runs truncated by eight other sources
  - **Symptom**: `GET /api/logs/actions` returns 13 recommendations and **zero** `noise` rows, though the two Bluetooth firmware signatures sit at 39,919 occurrences apiece — exactly Tier 2's scoped example. Forcing `confidence` to `HIGH` produces both rows correctly, so the family works and is simply unreachable
  - **Cause, and it is correct behaviour**: `log_actions` rule 4 suppresses every argument-from-a-count when confidence is `LOW`, and confidence is `LOW` because **118 runs in the window reported truncation**
  - **This entry's account of _which_ runs was wrong, corrected 2026-08-17 by Session 60 against `agent_runs`.** It said the 118 were "overwhelmingly `sysadmin-service` … on essentially every read". Measured over the same 7-day window: **kernel 103, mosquitto 14, sysadmin-service 14, sports_analyser 12, venture-assistant 11**, and 118 truncated runs out of **10,064** is **1.2 %**, not "essentially every". **104 of the 118 landed on one day**, 2026-08-12, as kernel reads — the Bluetooth firmware storm at 8.5 lines a second, which is a real ceiling nothing can remove
  - **So the fix named below would not have cleared this, and that is the more useful finding.** `log_trends._confidence` is `if coverage.runs_truncated > 0: return LOW` — **binary, not proportional**. Taking `sysadmin-service` from 14 truncated runs to 0 leaves 103 kernel ones and the report stays `low`. `SNAG-AGENT-008`'s volume half was fixed on 2026-08-17 anyway, on its own merits; this entry did not close with it
  - **What will actually happen**: the 08-12 spike falls out of the 7-day window around 2026-08-19 and confidence recovers **by itself**, then drops again on the next kernel storm. A family that is available only between storms is not available
  - **So one service's volume suppressing a family for every other source is still the shape**, but the service is the *kernel* and the volume is not a defect. Confidence is global to the report because the poll is global to the agent; there is no per-source confidence, and inventing one would need per-source truncation history, which `details['truncated_sources']` does carry but nothing aggregates
  - **The candidate fixes are therefore reordered.** Per-source confidence is now the **only** one that reaches the case, and the measurement above is what makes it worth the work: truncation on this box is normal for one source and rare for the rest, which is precisely the discrimination a global flag cannot make. A second candidate is to make the gate proportional rather than binary — 1.2 % of runs is not the same evidential state as 100 % — but that is a change to a rule about honesty and should not be made to unblock a demo
  - **Still deliberately not fixed by lowering the gate**, for the reason below, which the correction above strengthens rather than weakens
  - **Deliberately not fixed by lowering the gate.** A noise recommendation is an argument that a count is stable; serving it off a count known to be missing data is the opposite of what rule 4 is for, and the first thing it would recommend silencing is a signature it cannot see the whole of
  - **Found**: 2026-08-17 by Session 59, when the endpoint's first live run produced none of the rows the session was written to produce

- [P1] SNAG-DB-004: **the nightly retention purge has deleted nothing since 2026-08-08, while logging six successful purges a night** (2026-08-17, **fixed same day**)
  - **Symptom, measured before the fix**: `sysadmin.log_entries` held 626,906 rows reaching back to 2026-07-09 — 39 days into a declared 30-day retention — and `retention_config.last_purged_at` read `2026-08-08 03:00` for every table that has ever been stamped, with `disk_reviews`, `project_reviews`, `reliability_scores` and `unit_audits` never stamped at all. The 03:00 journal for 2026-08-17 carries `retention_purged … "table": "log_entries", "deleted": 175018` and five more like it, none of which happened
  - **Cause**: `KEEP_LATEST_PER` used the literal string `"true"` for "the whole table is one entity", spliced into `SELECT DISTINCT ON (true) id … ORDER BY true, generated_at DESC`. PostgreSQL reads a bare constant in `ORDER BY` as an **ordinal position**, so this is `non-integer constant in ORDER BY` — a parse error, not a runtime one. Parenthesising does not help; the parser strips it. `true::boolean` and `(1=1)` both parse, and neither was needed in the end
  - **Why nine days**: `run_retention` was a **single transaction over twelve tables**, so the twelfth table's parse error discarded the eleven before it. Each table logs its rowcount *before* the commit, so the failure was quieter than the success — the journal reported work that then rolled back. The one honest signal was `last_purged_at` frozen at 2026-08-08, a column nothing reads, plus a nightly `scheduler_job_error` covered by `SNAG-AGENT-008` below
  - **Why 1,866 green tests missed it**: `tests/test_retention.py` mocked the session, so every statement was asserted as a **string** and none was ever parsed. One test asserted `KEEP_LATEST_PER["project_reviews"] == "true"` — it pinned the broken literal exactly. No stronger string assertion could have helped; the thing that was wrong was SQL validity
  - **Fixed 2026-08-17** by three changes that are only jointly sufficient. `WHOLE_TABLE = None` is a **sentinel taking its own branch** (`ORDER BY … LIMIT 1`), removing the construct rather than repairing it — `NOT IN` over an empty subquery is `TRUE`, so an empty table needs no special case. Statement construction moved to a pure `purge_statement()`, which exists so `test_purge_statements_parse` can hand every statement to PostgreSQL via `EXPLAIN` — no scheduler, no clock, no row, and it covers the whole map because the next bad statement will be a different one. And **one savepoint per table** (`SysAdminAgent._execute`'s rule, one domain over) so a bad table costs only itself, with `last_purged_at` stamped *inside* the savepoint so a failed table keeps its old value and the column finally means what its name says
  - **Verified live rather than only against fixtures**: the real statements driven against the real database inside a rolled-back transaction — all twelve tables purge, 207,566 rows due to go at the next 03:00 (`log_entries` 626,906 → 451,888, `alerts` 685,649 → 675,531, `service_health` 101,826 → 89,258). The two `WHOLE_TABLE` tables delete 0 there, so the keep-latest semantic was forced separately with `cutoff = now()`: 4 rows → 3 deleted, 1 left, and the survivor is the newest in both. Nothing was deleted; row counts re-read after rollback are unchanged
  - **Both new guards were falsified before being trusted**: with the pre-fix code path restored, `test_purge_statements_parse` and `test_no_bare_constant_reaches_an_order_by` both fail with production's exact message, `non-integer constant in ORDER BY`
  - **Found**: 2026-08-17 by Session 59, while measuring how far back `log_entries` reaches — a question asked to bound Session 27's week-on-week window, not to audit retention

- [P1] SNAG-AGENT-008: **this service cannot see its own errors — every line it writes is journald priority 6** (2026-08-17, **fixed 2026-08-17** — volume half by Session 60, priority half by Session 61)
  - **Symptom**: `sysadmin.service` is a declared journal source in `services.yaml` at `severity_filter: warning`, and `sysadmin.log_entries` holds **0 rows** for it over the last 7 days — across nine consecutive nights on which `run_retention` raised a traceback logged at Python level `ERROR` (`SNAG-DB-004` above). No alert was ever raised. `journalctl -u sysadmin -p warning --since "7 days ago"` returns 0 lines
  - **Cause**: the daemon writes structured JSON to **stdout**, and systemd stamps captured stdout `PRIORITY=6` regardless of the `"level"` inside the payload. `read_journal` filters on journald's `PRIORITY` field, so `PRIORITY_MAP` maps every line to `info`, which is below `warning` — the filter discards the lot. Measured over the two-minute window containing the 03:00 failure: **277 entries, all `PRIORITY=6`**, the traceback among them
  - **A second, independent cause of the same blindness — volume, and this half was FIXED on 2026-08-17 (Session 60)**: `sysadmin.service` emitted **673 journal lines per 5 minutes** (676 over a 30-minute sample — steady, not bursty), so a read could hit the `-n 500` ceiling. The cursor advances over every line read, **before** the severity filter (correctly — that rule exists so info-level noise does not strand the resume point), so unread errors older than the ceiling are skipped permanently. Fixing the priority alone would not be sufficient
  - **What this entry originally claimed about the volume was wrong in three ways, all found by measuring rather than re-reading it.** It said the ceiling is hit "essentially every time"; it is hit **on the first read after a restart and at no other moment**. It said the cause is "an access-log line per HTTP request at INFO into the journal, a choice nobody revisited"; the dominant causes were a *duplicated* line and a tray polling a tab nobody was looking at. And the composition was never measured before being asserted
  - **Cause 1 — every request was logged twice.** `configure_logging` clears the **root** handlers, which does not reach `uvicorn.access`: uvicorn's default dictConfig attaches a handler to that logger directly and sets `propagate = False`, so it survived untouched and emitted a plain-text line beside the middleware's JSON one. Measured over ten minutes: **662 plain against 640 JSON access lines**, and 662 − 640 is exactly the **22 `/health` polls** the middleware excludes and uvicorn's did not — so `_EXCLUDED_PATHS`, which is `SNAG-API-002`'s fix, **has never worked**. `tests/test_logging.py::TestRequestLoggingMiddleware::test_excludes_health_endpoint` asserted the exclusion by patching the *middleware's* logger, i.e. the one that was already honouring it, so no test could have caught the emitter that was not
  - **Cause 2 — the tray fanned out `/details` for a tab nobody was looking at.** `ServicesTab` is constructed eagerly at tray startup (`sysadmin_tray/app.py`) and wired to `status_updated` unconditionally, so it issued one `GET /api/sysadmin/services/{name}/details` per systemd-backed service on every status poll whether or not the dashboard had ever been opened — ~32 services every `tray.status_poll_seconds`. **1,160 of the 1,347 lines in ten minutes, 86 %.** `DashboardWindow`'s own docstring promised the opposite (*"no background polling when hidden"*) and every other tab keeps it; `LogsTab` stops its timer in `hideEvent` and this one had no timer to stop, which is how it escaped notice — the polling was not scheduled, it was inherited from a signal that fires anyway
  - **Why the volume bit at exactly one moment, and it is the two halves of this snag multiplying.** `_read_journal_source` falls back to `since = "5m ago"` only when there is no cursor **and** `_resume_floor` returns `None`. For `sysadmin-service` the floor is *always* `None`, because the priority half means 0 rows are ever stored — so the durable resume mechanism is permanently disabled, every restart re-reads a five-minute window, and five minutes at 673 lines overflows a 500-line ceiling. The two halves were filed as independent and are multiplicative: fixing **either** stops the truncation
  - **Fixed 2026-08-17** by removing both causes rather than raising the ceiling. `logging.getLogger("uvicorn.access").disabled = True` in `configure_logging` (disabled rather than re-levelled: uvicorn logs access at INFO and nothing else, so `setLevel(WARNING)` would be silence spelled indirectly); and `ServicesTab._on_status` gates the fan-out on `self.isVisible()`, with `refresh()` fetching details for cards already held so a tab shown after polls it ignored does not sit blank for a poll interval. The middleware is the copy kept, because only it carries `method`/`path`/`status`/`duration_ms` as structured fields
  - **Both guards were falsified against the restored pre-fix code before being trusted.** `test_access_line_is_not_emitted` fails with production's exact line, `127.0.0.1:53994 - "GET /health" 200`; four of the seven `test_services_tab.py` tests fail with the whole service list. The logging half was additionally driven against uvicorn's **real** `LOGGING_CONFIG` rather than a reconstruction of it: `dictConfig(LOGGING_CONFIG)` then `configure_logging` gives `INFO:     127.0.0.1:53994 - "GET /health HTTP/1.1" 200 OK` before and `''` after, with nothing rerouted to the root handler
  - **Priority half fixed 2026-08-17 (Session 61), and the entry's own statement of the trade-off was wrong.** It said the two unit-file remedies both need `sudo` and that reading the `"level"` key in `read_journal` is "the only one needing no unit-file edit". Measured: `systemctl show sysadmin.service -p SyslogLevelPrefix` already reads **`yes`**, because `SyslogLevelPrefix=` **defaults to true** in systemd. So the prefix remedy needs no unit edit and no `sudo` either — it sits on exactly the footing the entry credited only to the reader-side hack, while fixing the artefact rather than one consumer's view of it
  - **Verified against a transient unit before any code was written**, not read off the documentation: `<4>{…}` on stdout arrives as `PRIORITY=4` and journald **strips the prefix**, so `MESSAGE` is byte-identical to the unprefixed line and `log_signature`, `alert_title` and the stored `raw_line` need no change
  - **Why the producer and not the reader.** Parsing `"level"` inside `read_journal` leaves the journal itself lying: `journalctl -u sysadmin -p err` still prints nothing, and so does any `OnFailure=` hook or anyone reading by hand. It would also put a special case for **one** source into a reader serving fourteen, keyed on this application's log format — and `sysadmin.service` is the only JSON-writing journal source on this box (measured: `estate-manager-api` wrote 266 plain lines in six hours, alfred none), so the branch could never pay for itself
  - **`systemd.journal` was rejected on measurement, not argument**: `from systemd import journal` raises `ImportError` in the venv, so it is a new native dependency, and it replaces stdout-JSON with a different transport rather than repairing it
  - **The prefix is bound to the JSON format, and the gate is a precondition rather than a proxy.** A level prefix marks one line, and only the JSON formatter guarantees one line per record — `json.dumps` escapes newlines, so a traceback travels on the line whose level describes it. Under the text formatter the first line would be stamped `ERROR` and the body left at `info`: one fault split across two priorities, which is worse than the uniform `6` it replaced because it *looks* fixed
  - **A second emitter was in scope and it is the one carrying the errors.** `uvicorn.error` has no handler and propagates only as far as `uvicorn`, which keeps a plain-text stderr handler with `propagate = False` — exactly the shape `uvicorn.access` had. So `Exception in ASGI application` and the traceback of every unhandled 500 went out as plain text at `PRIORITY=6`, and no prefix on *this application's* logger could have reached them. **Rerouted, not silenced**, deliberately the opposite verb from its sibling three lines up in the same function: the access line duplicates a structured line the middleware already writes, so the second copy is waste; uvicorn's error line has no second copy anywhere, so silencing it would delete the only record an ASGI crash leaves
  - **Session 60's own guard was asserting the opposite, and passing.** `test_only_the_access_logger_is_silenced` claims `uvicorn.error` reaches the root handler; its fixture rebuilds `uvicorn.access` and **not** its parent, leaving `uvicorn` with no handler and `propagate = True`. Driven against uvicorn's real `LOGGING_CONFIG` on 2026-08-17, the record went to uvicorn's own handler as `ERROR:    address already in use` and the root buffer stayed **empty**. A green test asserting a decision the box does not implement is `test_excludes_health_endpoint`'s defect one logger over, shipped by the session that found it — so the new class drives the real `dictConfig` and the old test's docstring now says what it does not cover
  - **Verified live rather than only against fixtures, and without the `sudo` the deploy needs.** A transient user unit ran the real `configure_logging` and emitted four records; journald recorded `INFO→6`, `WARNING→4`, `ERROR`+traceback→`3`, `uvicorn.error→3`, and the **real `read_journal`** at `severity_filter: warning` returned **3 entries where it has always returned 0**, prefix stripped, cursor set. The round trip is pinned by a test against `journal.PRIORITY_MAP` for all five levels, because two maps that can disagree about one fact is this repository's recurring defect
  - **Both new guards were falsified against restored pre-fix code**: reverting the formatter fails 12 tests including every level case, and removing the uvicorn reroute fails exactly the two that name it
  - **Why this is the interesting half of `SNAG-DB-004`**: the alerting path was not merely unused, it was structurally unreachable. This repository's rule is that the monitor must not own the things it monitors — but it does monitor itself, via `self_monitor` and its own `services.yaml` entry, and that self-observation has a hole exactly where its own stack traces are
  - **What the entry said before it was fixed, kept because the premise was the interesting part**: "the candidate remedies are not equivalent and the choice is not obvious: `SyslogLevelPrefix=` plus a prefixing handler, `systemd.journal.JournalHandler`, or reading the `"level"` key out of the JSON payload in `read_journal` — the last being the only one needing no unit-file edit and therefore no `sudo`". Two of those three clauses were wrong, and one `systemctl show` settled both. A trade-off written from documentation rather than from the box sent the choice toward the weakest of the three
  - **Still owed: `sudo systemctl restart sysadmin`.** uvicorn serves start-time code, so `log_entries` holds 0 rows for this source until the daemon is restarted — the same restart the volume half has been waiting on since 2026-08-17. Both halves land together
  - **Found**: 2026-08-17 by Session 59, by asking why nine nights of `ERROR` produced no alert row — the question the retention fix raised rather than answered

- [P2] SNAG-LOG-003: **this daemon's own alerts will carry a JSON document where the message should be** (2026-08-17, opened by Session 61)
  - **Symptom, measured before it can happen**: with the priority half fixed, `sysadmin.service`'s errors finally reach `log_entries` — and `entry["message"]` is the whole formatted JSON line, because that is what journald's `MESSAGE` field holds for a service that logs structured JSON to stdout. `alert_title` therefore produces `Log error: sysadmin-service — {"timestamp": "N-N-N N:N:N,N", "level": "ERROR", "logger": "sysadmin.core.retention", "message": "retention_purge_failed", "service": …` at 252 of the 255 available characters, and `alert.message` reaches a notification body verbatim
  - **Detection is unaffected and that is why this is P2 rather than P1.** Driven through the real `read_journal` and the real `alert_title`: **two distinct faults produced two distinct titles**, so the signature discriminates and dedup works. `signature()` normalises the timestamp's digit runs to `N`, and the traceback's line numbers with them, so repeats of one fault collapse correctly. What is wrong is legibility, and only for this one source
  - **The obvious fix is the coupling this session's remedy was chosen to avoid.** Sniffing a leading `{` in `read_journal` and lifting out `message`/`level` puts a special case for one source into a reader serving fourteen, keyed on this application's log format — precisely the argument that sent the priority half to the producer instead
  - **The candidate that is not a coupling** is to make it a **declared property of the source** rather than a guess about it: `log: { type: journalctl, severity_filter: warning, format: json }` in `services.yaml`, with the unwrap applied only where declared. That is the shape `kind:` already has in the same file — the reader would be honouring a declaration, not recognising an application. Note it also gives `metadata['logger']` for free, which nothing currently has
  - **Deliberately not bundled into Session 61.** It is a second decision about a different module, and the fix it needs is a schema change to `services.yaml` rather than a line in a formatter
  - **Not observable until the restart**, since `log_entries` holds 0 rows for this source until `sudo systemctl restart sysadmin` runs — so the first real instance is also the first chance to check the title against a live notification

- [P3] SNAG-ESTATE-009: **the dev-server quietening is only as good as the sweep's age, and the window is six hours wide** (2026-08-17, opened by Session 57)
  - **Symptom**: `judge_audit_findings` quietens a ports breach to `info` when the stored sweep attributes the port to a session scope. The sweep runs six-hourly (`agents.service_discovery.scan_interval_hours: 6`) and the estate judge hourly, so a dev server started inside a sweep window has no attribution, reads as an ordinary unclaimed listener, and is raised at `warning` with a toast
  - **Measured, not assumed**: the two live rows were raised at 12:07 off a 06:07 sweep. They happened to be attributable because the editor had been open across the sweep; had VS Code been opened at 09:00 the same rows would have spoken loudly and stayed loud until the 12:07 sweep, then held (dedup suppresses the re-raise) — so the toast happens whatever the next sweep learns
  - **Two fixes considered and both rejected in Session 57.** Running `ss` inside the judge closes the window exactly and is refused in writing by `EstateJudgeAgent._attribution`: two calls at two moments give two answers to one question with neither surface saying which it used, which is the defect the cross-domain read exists to avoid. Raising the sweep to hourly is one config line and pays six times the sweep's cost across every consumer of `unit_ports` for the benefit of one annotation
  - **Why P3**: the failure is one extra toast at the start of a dev session that begins mid-window, against the two-per-session-forever this repository has just removed. It is a lower bound of the kind already published elsewhere — `at_window_edge`, `observed_from`, `standing_days` — and the honest fix is that `details['holder']['observed_at']` already says how old the evidence is
  - **What would change the ranking**: a second family learning to read `PortAttribution`. One consumer tolerating a stale window is a stated limit; three consumers each tolerating it separately is the copied-filter shape `snapshots.py` exists to prevent

- [P3] SNAG-RELOAD-001: **after a reload, the config object can hold a scheduler setting the running scheduler does not obey, and nothing says so twice** (2026-08-15, **fixed same day**)
  - **Symptom**: `sysadmin/reload.py` installs the whole `AppConfig`, so `get_config().agents.sysadmin.health_check_interval_seconds` reads the new value while the APScheduler job keeps the trigger built at startup. Measured live this sitting: edited to 999, the config object returned 999 and the job stayed on 300 s
  - **Before the reload existed this divergence could not occur** — startup built the config object and the scheduler from one read, so the two always agreed. It is a cost the reload introduces, not a pre-existing fault it exposes
  - **It is reported, once.** `ReloadReport.requires_restart` names every such leaf in the response body and in a `WARNING` log line. That is the whole mitigation, and it is the shape Session 39 spent itself removing: a warning that fires once is indistinguishable from one that got fixed. An operator who reloads and walks away has nothing that still says the scheduler is not obeying config.yaml
  - **Two candidate fixes, neither costed.** Store the last `ReloadReport` and serve it (cheap; but a field nobody polls is the `SNAG-CFG-001` shape), or raise it as an alert row so it reaches the tray, the DND windows and `/api/sysadmin/alerts` through plumbing that exists. The second is the right shape and needs a settled dedup and resolve lifecycle before it is written — a family added without one is how 51,924 rows happened
  - **The real fix is upstream of both**: `Scheduler` exposes no `reschedule_job`, so nothing re-times a job. Adding it would shrink `RESTART_ONLY` to the socket, the engine and the logging setup and make this snag mostly disappear, rather than reporting it better
  - **Found**: 2026-08-15 by running the mixed edit against the live config.yaml — one live field and two restart-only ones in one file — which is the case the apply-and-name decision was taken for
  - **Fixed 2026-08-15 (Session 50), by the third option.** Neither candidate mitigation was built: storing the report and raising an alert row both describe the divergence, and the entry's own last line says the divergence can be removed instead. `sysadmin/core/jobs.py` holds the plan (`plan_jobs`: an `AppConfig` -> the jobs it asks for) and `apply_jobs` reconciles a scheduler with it; `Scheduler` gained `sync_interval`/`sync_cron`/`remove_job`. The lifespan and the reload now call **the same function**, so the schedule at startup and the schedule after a reload cannot be produced differently. `RESTART_ONLY` went from **fifteen leaves to two prefixes** — `service` and `database`, covering the socket, the logging setup and the engine
  - **The obvious implementation was wrong, and in the direction that matters.** Re-applying every job on every reload is one line shorter and breaks the schedule: `reschedule_job` recomputes the next fire from *now*, so a 24-hour job would sit permanently 24 hours from the most recent reload — which on a box being poked at is never. That is `agent_first_run_delay_seconds`'s failure (SNAG-AGENT-003) with a reload standing in for a restart. A job whose trigger has not moved is therefore left **untouched**, decided by comparing against the **live** trigger rather than a remembered plan: a remembered plan is a second statement about what the scheduler is doing, which is what this entry was
  - **`jobs_synced` is the field that keeps the report honest.** `jobs_retimed: []` means "nothing needed re-timing" when it is true and "the scheduler was never looked at" when it is false — `UnitScanResponse.ports_checked`'s rule, one domain over. Without a syncer the reload falls back to Session 49's behaviour exactly, reporting every job leaf that moved, and that fallback reads its path list from the plan (`JOB_CONFIG_PATHS`) rather than restating it
  - **Verified live** on 2026-08-15 against the real `config.yaml` and a real `BackgroundScheduler`, in-process and touching neither the daemon nor the file: 999 s became `interval[0:16:39]`, `retention_purge` moved 03:00 -> 04:00, a disabled `estate_judge` had its job **removed** and re-enabling **added it back with the first-run delay**, and `requires_restart` came back `[]` where Session 49 reported three leaves. Reloading the same file twice re-timed nothing
  - **What it does not fix**: `RESTART_ONLY`'s two survivors are genuinely immutable in-process. A port or a database URL change still needs `sudo systemctl restart`, and the report still says so
- [P0] SNAG-DB-001: the live database was a migration behind, and it caused a **39-hour monitoring blackout** nobody saw (2026-08-10, fixed same day)
  - **Symptom**: **Zero rows were written to `service_health` between 2026-08-08 17:34:54 and 2026-08-10 09:07:25.** Not degraded — absent. `/api/sysadmin/status`, the tray grid and reliability scoring were all serving from a table that had stopped receiving data a day and a half earlier
  - **Cause, in three parts, each individually survivable**:
    1. `alembic current` reported **008** while the repository head was 009. Migration 009 — which adds `'skipped'` to `chk_health_status` — was written and committed on 2026-08-08 (Session 35 Phase 3) and never applied. **Nothing applies migrations**: no script, no `ExecStartPre`, no CI step. It is a manual `uv run alembic upgrade head` a human has to remember
    2. `sysadmin.service` restarted at 17:36:31 on 2026-08-08, two minutes after the last successful check, and picked up the new `services.yaml`. `venture-chat-large` is declared `kind: static` with `monitor: false`, which records `skipped` — the exact value the un-applied migration was meant to permit
    3. **One rejected row killed all nineteen.** `SysAdminAgent._execute` `session.add`s every service's result into one session and `BaseAgent.run` commits once, so the `CheckViolationError` on `venture-chat-large` aborted the whole transaction. A single deliberately-unmonitored service took the entire health check down with it
  - **Why nothing noticed**: the daemon logged `agent_run_failed` on every run — **29 times today alone**, once every five minutes from 06:37 to 09:02 — and nothing reads those. The alerting path is the thing that broke, so it could not report its own failure. `verify_connection` checks that the database answers, not that it is the schema this code was written for. The schema drift guard connects to the live database and would not have caught it either: it explicitly skips `alembic_version`, and `compare_metadata` does not diff CHECK constraints — verified 2026-08-10 by restoring the pre-009 constraint inside a rolled-back transaction and getting an empty diff, with the model declaring `'skipped'` and the database rejecting it
  - **Impact**: ~39.5 hours wall clock, of which roughly **18 hours the daemon was up and failing every five minutes** (the box was asleep for the rest). No health history, no alerts, no reliability data. `GET /api/services/reliability` computes live from `service_health`, so it scored the estate off a table with a day-and-a-half hole in it
  - **Fixed**: `uv run alembic upgrade head` on 2026-08-10 applied 009, 010 and 011. Checks resumed at 09:07:25, first `skipped` row written successfully. **This was found by accident** — while tracing why the drift guard had stayed green, not by anything designed to catch it
  - **The detection gap was the real defect, and is closed as of 2026-08-13 (Session 43)**, all three parts: (1) `sysadmin/core/schema_guard.py` compares `alembic_version` against the packaged head at startup and **refuses to boot** on a mismatch, so the unit enters `failed` and `sysadmin-failed.service` announces it — the Session 39 machinery's second caller; (2) one `session.begin_nested()` per service in `SysAdminAgent._execute`, with a rejected row recorded as `status="error"` rather than costing the run, so a blackout becomes one bad tile; (3) `sysadmin/monitor/failures.py` raises `{agent} agent failing` off consecutive `agent_runs` failures, on the shared ladder. Note **(3) was not implementable when this entry was written** — `_record_outcome` wrote `status='failed'` into the transaction the failure had already destroyed, so there were no failure rows to read; Session 41's three-transaction split created them. The fourth item on the list, a live-database test path for the shared snapshot query, is still open. See [tasks.md](tasks.md)

- [P2] SNAG-PROJ-013: `ImbaBots` heads its handoff with no ISO date, so the Stop hook will block its next code session (2026-08-11)
  - **Filed first with the wrong diagnosis, corrected the same day.** The original entry read "commits without ever moving its handoff date — either the hook is not firing there or that work is not done through Claude Code sessions", and both alternatives are false. Recorded rather than quietly rewritten, because the mistake was to reason from an endpoint's output instead of opening the repository
  - **What is actually true**: ImbaBots' last session is `edbd8c2` (2026-08-07 12:03), which changed 22 `game/` files **and** `docs/handoff.md` in one commit — the habit was kept. `HANDOFF.md`'s mtime is still that timestamp because nothing has run there since. The only later commit, `7144fc5` on 2026-08-10, is the estate migration promoting `docs/handoff.md` to the root, run from *this* repository. ImbaBots' first scan in the momentum window falls after 12:03 on 2026-08-07, so that date is its **baseline** — a state, not a transition — and `sessions: 0` is the correct reading, not a measurement failure
  - **The residual defect is smaller and elsewhere**: the first heading is `# Handoff — M5 (Tier 2) · ⚠ …` with no `YYYY-MM-DD`. `~/.claude/hooks/require-handoff.sh` greps the first heading for today's date, so **the next code-changing session in ImbaBots will be blocked until someone adds one** — the hook cannot be satisfied by the document as it stands. Secondarily, an undated heading means `handoff_date_source` is permanently `mtime` there, so every session ImbaBots ever contributes reads `unverified`
  - **Fix, and why it must not be applied by hand**: an ISO date in the first heading resolves both halves — but it has to be written by the session that earns it. `require-handoff.sh` fires on *any* dirty tree or *any* commit that day, not only code, and demands **today's** date; so editing ImbaBots on a day nobody worked there forces a heading dated to a session that did not happen. On the next scan that is a handoff-date transition, i.e. **a phantom session in `GET /api/projects/momentum`** — and since `docs(…)` is not in `code_commit_ignore`, the doc commit itself would set `last_commit_at` to that day and the phantom would read as *landed*. The `unverified` machinery exists to stop mtime inventing a session; back-writing a date does the same thing deliberately. Considered and rejected 2026-08-11, along with adding `docs(handoff)` to `code_commit_ignore` to soften it — that changes staleness measurement estate-wide to accommodate one edit, and the row is still a phantom
  - **Therefore: no action here, and none in ImbaBots until it is next worked on.** The hook will stop that session, on a real date, and its block message already states that the first heading must carry today's date — the mechanism working, not a trap. Close this entry when a dated ImbaBots handoff appears. **Not** a change to this codebase either: the `commits_without_sessions` field considered in the original entry would have widened the contract to describe a defect that does not exist

- [P1] SNAG-AGENT-003: the file organiser has run **once in its life**, and its own stall alert has been open for a day (2026-08-11)
  - **Symptom**: `sysadmin.agent_runs` holds exactly **one** `file_organiser` row — `2026-08-06 12:05:41`, `completed`, 29.63s — against `log_aggregator` 31,431, `sysadmin` 6,052, `service_discovery` 26 and `project_organiser` 100. Every other agent ran on 2026-08-11; this one last ran five days earlier. `agents.file_organiser.enabled: true`, `scan_interval_hours: 24`. A `file_organiser agent stalled` alert has been open since 2026-08-10 08:07 and was **not acted on**, which is the second half of the defect
  - **Found**: by Session 36's `facts.stale_sources`, which flagged `filesystem` as measured 2026-08-06 — reached from the data side, independently of the alert that had already been raised from the agent side. Two routes to one fact, and neither had been read
  - **Cause**: unconfirmed, deliberately not chased in Session 36. The candidate is the interaction the config file already documents at `config.yaml:109` — *"this is why file_organiser, at 24h, had zero recorded runs"* — where an APScheduler interval job counts from process start, so an agent whose interval is longer than the gap between daemon restarts never reaches its own schedule. `agent_first_run_delay_seconds: 60` was added to work round exactly that, and the daemon restarted at 2026-08-11 11:21 UTC without producing a run, so **either the first-run delay is not firing for this agent or the run is failing before it records**. Both need checking against the live scheduler; the single recorded run is `completed`, so nothing has ever recorded a failure
  - **Impact**: `GET /api/files/*` has served five-day-old figures throughout — `reclaimable_mb: 34849`, `stale_project_dirs: 102` — with nothing on those endpoints saying so, and the weekly disk review narrates from the same stale audit. Same shape as SNAG-DB-001: a monitoring component silently stopped, and the estate reported the last thing it knew as though it were current
  - **Fix**: establish which of the two candidates it is before changing anything — a scheduler that never fires and an agent that dies silently need opposite fixes. Then make the difference visible: `GET /api/sysadmin/self` already reads `agent_runs` and the stall alert already fired, so the missing piece is that **nothing forces the alert to be seen**, not that nothing detected it
  - **Second half addressed 2026-08-11 (Session 39), first half still open.** The escalation ladder now re-raises a stall as `critical` once the `warning` has stood `escalate_after_hours` unresolved, and `critical` is the only severity the tray renders as a notification that does not expire — so this alert would have become persistent on 2026-08-11 09:07 rather than remaining a single toast from the day before. That fixes *being told*. **It does not fix the file organiser**, which has still run once in its life, and the two candidate causes above are still unseparated. Note what the ladder changes about the evidence: from now on a stall of this kind leaves two rows and a `details.first_alerted_at`, so the next occurrence records how long it stood rather than only that it happened
  - **Cause established 2026-08-12 (Session 41), and it is neither candidate.** Separating them took one `journalctl` call. The scheduler fires correctly and the agent *succeeds*; the transaction is killed underneath it. At the last daemon start the job was registered (`scheduled_interval_job file_organiser_scan {"hours": 24}` at 17:08:12), ran on `agent_first_run_delay_seconds` as designed, and logged `agent_run_completed duration_s 117.71 findings 25317` at 17:11:10 — followed immediately by `InterfaceError: connection is closed` on `UPDATE sysadmin.agent_runs SET status='completed'`. `SHOW idle_in_transaction_session_timeout` → **`1min`**. `BaseAgent.run` inserted the `running` row and **flushed** it, opening a transaction, then handed the *same* session to `_execute`, which spent 118 seconds in `asyncio.to_thread` touching no database. PostgreSQL terminated the backend at t+60s and every write of the run rolled back — the audit, and the run record itself
  - **This is a `BaseAgent` defect that only the slowest agent is slow enough to hit.** sysadmin 13 s, project_organiser 2.3 s, service_discovery 0.2 s, log_aggregator 0.3 s; file organiser 118 s. And **the one surviving run is the evidence, not the exception** — 2026-08-06 took `29.63s`, the only run in the agent's life to finish inside the timeout. The rule was already written down one layer up, in `files/review.py`: *"commit the read transaction before calling the LLM — this host sets `idle_in_transaction_session_timeout=1min` and inference takes longer."* Learned for the LLM, never generalised to the framework beneath it
  - **"Nothing has ever recorded a failure" was not reassurance, it was the second symptom.** The `except` branch sets `status='failed'` on a row living in the killed transaction, so the failure record dies with the run it records. An agent failing this way is indistinguishable from one that was never scheduled — which is what `GET /api/sysadmin/self` reported for five days, correctly, off a table that was being emptied
  - **Fixed 2026-08-12 (Session 41)**: `BaseAgent.run` now runs in **three** transactions — the `running` row commits alone, `_execute` gets a session that has never been flushed (so its transaction opens at its first statement, not 118 seconds earlier), and the outcome is an `UPDATE` by id from a third. Free, because `UUIDPrimaryKeyMixin` sets `default=uuid.uuid4` client-side. Rejected: raising the timeout on scheduler connections — one line, but it suppresses the host's deliberate guard on the connection that idles longest and still leaves a failed run unable to record itself. **Proven on the box the same day**: after the restart the file organiser recorded **3 completed runs plus one `running`**, against one run in the agent's entire life before today, and `filesystem_audits` went from a single 2026-08-06 row to four. The `running` row is the documented new behaviour — a run in flight is now visible rather than absent

- [P1] SNAG-AGENT-004: **26,270 alert rows for four services that no longer exist can never be resolved or purged** (2026-08-11)
  - **Symptom**: unresolved alerts, counted live — `personal-assistant` **9,748**, `personal-assistant-frontend` **9,748**, `redis` **4,950** (newest 2026-03-07), `ollama` **1,824**. None of the four appears in `config.yaml` or `services.yaml`; PA and ollama were retired 2026-07-24. `GET /api/sysadmin/alerts` and the tray badge carry them, and three `Critical disk usage on /` rows from July sit alongside them although the disk has been at 68 % since August
  - **Cause, two halves that only bite together**. `SysAdminAgent._handle_status` calls `resolve_alerts(session, service_name)` **inside the per-service check loop** ([agent.py:391](../../sysadmin/monitor/agent.py#L391)), so recovery is only ever observed for a service still being checked. A service removed from configuration is never checked again, so it can never be seen to recover. And `run_retention` purges alerts `WHERE ... AND resolved = TRUE` ([retention.py:85](../../sysadmin/core/retention.py#L85)) — deliberately, so an open incident is never deleted from under its reader. An unresolved row for a deleted entity therefore lives for ever
  - **This is a defect the project side already fixed, on the service side.** `ProjectOrganiserAgent._resolve_recovered` exists precisely because "a project deleted from disk never appears in a scan, so it can never be observed *recovering*, and a per-project loop leaves its alert unresolved forever" — that is how 1,664 rows accumulated. The monitor has the same shape at **16× the scale**, and the fix pattern is already in the codebase: ask the inverse question set-based (which open service alerts would this run *not* raise), rather than looping over the survivors
  - **Found**: by checking live state at the end of Session 39 while confirming the escalation ladder had fired. Nothing surfaced it — which is the point: these rows are indistinguishable from live incidents on every surface that shows them
  - **Impact**: worse than table size. A permanently-open `critical` trains the reader to dismiss the alerts list, which is the exact behaviour Session 39 spent a day trying to earn back. It also poisons any future "is anything wrong right now" check built on `resolved = false`
  - **Fix**: a set-based resolve for service alerts, mirroring `_resolve_recovered` — and a one-off backfill for the four retired services, which is a data change and needs an explicit call. Note the retention rule must **not** be relaxed to purge unresolved rows: that would hide the symptom and delete live incidents with it
  - **Scope was understated by about twenty times, found on re-counting 2026-08-12.** Retired services are **five**, not four — `nuxt-frontend` (224 rows) was missed — and `redis` is 6,283 rather than 4,950, so the service half is **27,827**. Beside it sit **24,097** resource-threshold rows with **no resolve path at any point in this application's life**: `Critical disk usage on /` is **13,971 open rows**, last raised 2026-07-26, against a disk that has been at 68 % since; `High disk usage on /` another 9,530. `_check_thresholds` raises unconditionally and nothing has ever resolved what it raises. A condition that recovered and could not be observed recovering is the *same* defect as a service that was retired, so it is closed by the same statement rather than filed separately
  - **Fixed 2026-08-12 (Session 41)**: `SysAdminAgent._resolve_recovered` asks the inverse question over `RESOLVABLE_TITLE_PATTERNS` — **patterns, not the configured service list**, because a set built from configuration cannot contain a deconfigured service, which is the entire defect. The per-service `resolve_alerts(session, service_name)` is gone, and with it a second unfiled bug: it was a substring `ilike`, so `venture-chat` recovering also closed `venture-chat-large`'s alerts. Three families are excluded because each has a lifecycle owner already — `% agent stalled` (`stalls.py` escalates *off* the quiet row staying open), `% failed` (`unit_failure.py` writes it dead, the lifespan resolves it alive), `Unusual % usage` (resolved by id). Verified against the live table: **51,924 rows in the population, and the only `agent='sysadmin'` row left open is the file organiser's stall**, which is still true
  - **The obvious implementation was written first and refuted before it shipped.** Excluding only "titles this run raised" is the direct reading of the project-side pattern and is wrong here: `_degraded_counts` is in memory and resets on daemon restart, so the first run after one raises nothing for a service degraded for hours — the alert would be resolved as *recovered* and re-raised two checks later, announcing a recovery to the tray for a fault that never went away. Services are resolved only when this run measured them healthy; resource thresholds keep the raised/not-raised test, because a mount either breached or did not and no streak is involved. Two rules in one statement, and the docstring says which half is which
  - **Backfill ran itself, and the numbers landed**: **51,976 rows resolved** on the first runs after the restart, taking `agent='sysadmin'` unresolved from 51,925 to **43**. No script was written — chosen so the fix would be proven by doing exactly what it will do for ever after

- [P1] SNAG-ESTATE-001: **two retired PersonalAssistant units have restart-looped 52,178 times since the reorganisation, and hung the kernel once** (2026-08-12)
  - **Symptom, live at the time of writing**: `personal-assistant-idle-watcher.service` **NRestarts=34,517** and `personal-assistant-worker.service` **NRestarts=17,661**, both `enabled`, both in `activating (auto-restart)`, both climbing at roughly 1.3 restarts per minute — the counters rose by 9 and 5 during a seven-minute investigation. PA was retired 2026-07-24 and its repository moved to `~/projects/archive/`; the unit files still name `/home/gaddi/projects/PersonalAssistant/scripts/run-worker.sh`, so every start fails `203/EXEC` and systemd immediately schedules another. **6,812 failure/restart journal entries today alone** for the worker
  - **Started 2026-08-04 21:13:18**, the first `Unable to locate executable` in the journal — the `~/projects` reorganisation (Phase 1, same day). Nothing was checking that moving a repository leaves no enabled unit pointing at the old path, and 8 days later nothing had noticed
  - **It has already cost something.** On **2026-08-08 04:10–04:14** the kernel logged RCU stalls followed by hung-task warnings: `INFO: task (idle-watcher.):658024 is blocked on a mutex likely owned by task (run-worker.sh):658025`, both blocked **>245 seconds**, alongside two wedged kworkers (`kworker/u128:0`, `kworker/11:1`) and `rcu_preempt detected expedited stalls`. A restart storm from two dead units deadlocked against each other and stalled RCU on a 32-thread box. That is 13 hours before `SNAG-DB-001`'s monitoring blackout began (17:34 the same day) — **no causal link is claimed**, and the shared date is recorded only so nobody assumes one later
  - **The detector found this perfectly and nobody read it.** `GET /api/units/status`'s latest sweep classifies both as `category: "orphaned"`, names the `dead_path`, resolves where the project went (`project_path: /home/gaddi/projects/archive/PersonalAssistant`) and states the cause in plain English: *"WorkingDirectory /home/gaddi/projects/PersonalAssistant does not exist — systemd fails the start job, so this unit cannot run"*. There is an open alert, `Unmonitored systemd units: 17 findings`, raised 2026-08-11. **The diagnosis was complete, correct, machine-readable and eight days old.** This is the estate pattern in its purest form to date — better evidence than any of the six instances estate-manager ADR-0001 cites — because here the measurement was not merely ignored, it was *articulate*
  - **`activating (auto-restart)` is exactly the invisible state Session 39 fixed on one unit and one unit only.** That session established that `Restart=always` without `StartLimitBurst` means a crash loop never reaches `failed`, so no `OnFailure=` can fire and `systemctl is-failed` says nothing is wrong. It fixed `sysadmin.service`. These two units are the same defect, on the same box, running the whole time — and the estate has **no rule** requiring the pairing of anything else
  - **Stopped 2026-08-12** on the owner's instruction: `systemctl --user disable --now personal-assistant-worker.service personal-assistant-idle-watcher.service`. Both went `inactive (dead)` / `disabled` with `NRestarts` reset to 0, and **no unit on the box is in `activating` any longer**. **All five `personal-assistant-*.service` files were then removed** from `~/.config/systemd/user/` (backend, frontend, idle-watcher, tauri, worker) and the user daemon reloaded: `systemctl --user list-unit-files` now matches nothing, with 0 activating and 0 failed, and every other user service still running. Copies are in the session scratchpad rather than the repository — they are the owner's systemd state, not this project's. Reading all five before deleting was worth it: **every one is `Restart=always` or `on-failure` with no `StartLimitBurst`**, so the invisible-loop shape Session 39 fixed on `sysadmin.service` was present five times over on this box, and the two that happened to have a dead `ExecStart` are simply the two that got the chance to demonstrate it
  - **The durable half was two things. The second is fixed (2026-08-14); the first is still open.** What remains is the retirement checklist that includes `systemctl disable` — a process, not code, and not this repository's to enforce
  - **"Make an orphan finding speak" — fixed 2026-08-14.** The sweep now measures whether an orphan is **armed** (systemd will start it) and the agent gives each armed one its own alert row naming the unit and scope, beside the roll-up rather than instead of it. `GET /api/units/*` stays GET-only and advice-only; what changed is that the dangerous subset no longer waits to be fetched
  - **The obvious rule for "will it loop" was wrong, and the live units are what proved it.** "`Restart=` with no `StartLimitBurst=`" reads as sound and is wrong in both directions. `personalassistant-backend.service` declares no start limit, so systemd's defaults apply (`DefaultStartLimitIntervalSec=10s`, `DefaultStartLimitBurst=5`) — a limit **does** exist. It also sets `RestartSec=10`, which puts starts ten seconds apart, so five can never fit inside the ten-second window: the limiter is unreachable and the unit restarted 34,517 times without once entering `failed`. In the other direction, a bare `Restart=always` restarts every 100ms by default, five starts fit easily, and the loop *is* terminal — the naive rule would have raised a critical on most of this box's healthy services on its first run. The real test is arithmetic: `RestartSec × (StartLimitBurst − 1) < StartLimitIntervalSec`. It is the same arithmetic Session 39 did by hand for `sysadmin.service` (`StartLimitIntervalSec=600` against `RestartSec=10` → 40s < 600s), which is why that fix works, and `alfred-backend.service` passes it too on a hand-written `StartLimitIntervalSec=60`
  - **Both signals are pure**, so `sysadmin/units/scan.py` keeps its no-subprocess promise: "enabled" is an enablement symlink under a `*.wants/`/`*.requires/` directory the sweep already walks (matched on link *name*, so a dangling link left by an `rm` without a `disable` still counts), and the restart arithmetic is four keys of text it already parses. Cross-checked against `systemctl is-enabled` on every unit on this box on 2026-08-14: they agreed
  - **Live on this box the day it landed: 1 armed orphan of 6.** `garmin-sync.service` (user, enabled, `WorkingDirectory` gone) → one `warning` row. The four `Restart=always` orphans are harmless *only because someone disabled them*, so they stay in the roll-up as debt with the shape recorded on the finding. Verified against the live database in a rolled-back transaction: 5 runs of one fault wrote **2** rows — raise, dedup, escalate `warning`→`critical` when the loop appears, hold, resolve once on clearing — with the roll-up untouched and 0 residue
  - **What is deliberately not fixed here** is the general case: 15 of the 18 units on this box with a `Restart=` policy cannot reach `failed`, including every live service except `sysadmin`, `alfred-backend` and `alfred-frontend`. Filed as `SNAG-UNITS-002` rather than alerted on, because 15 criticals is not "speaking". **Fixed 2026-08-15** and the judgement held: it shipped as ranked advice under `GET /api/units/actions` with no alert row of its own, and the population had grown to 17 of 20 by then

- [P1] SNAG-AGENT-005: **598,091 log-aggregator alert rows can never be resolved, because they are events wearing a state's clothing** (2026-08-12, **fixed 2026-08-12**)
  - **Fixed** in Session 42, as a *raise* rule. The alert is now keyed on a **normalised fault signature** rather than on the source, so one row is raised per distinct fault and repeats bump `details['occurrences']` instead of inserting. Measured against the live storm the same afternoon: **2,000 kernel error lines in ten minutes → 2 alert rows**, and the titles finally say what broke (`Log error: kernel — Bluetooth: hciN: Failed to load firmware file (-N)`) where before every one of them read `Log error: kernel`. The 598,091 existing rows were resolved as `superseded` in one statement, taking the whole table's unresolved count to **2**
  - **Why the key is the signature and not the source, which is the part worth keeping.** Deduplicating on the existing title was the obvious version and it is wrong: `Log error: kernel` is shared by every kernel error whatever it says, so the Bluetooth storm would have held the single open row while the RCU stall and the USB enumeration failure — **both in the same live 30-day window** — went unannounced. Trading half a million rows for a mask over every other kernel fault is not a fix. `sysadmin/monitor/log_signature.py` strips digit runs and hex literals, which collapses 594,779 Bluetooth lines to 2 signatures and 110 USB lines to 2, while leaving all six distinct faults distinguishable. Normalisation loses `error -110` versus `error -71`; nothing is actually lost, because the alert's `message` column carries the last verbatim line and `occurrences` carries the count that used to be expressed as row volume
  - **Resolution is time-based, and it had to be.** `_resolve_quiet` closes a row whose signature has not been logged for `alert_quiet_minutes` (15, i.e. 15 polls). The `SNAG-AGENT-004` inversion — "which open alerts would this run not raise?" — is meaningless for an event, since the answer is "all of them, one run later". Anything the run observed is excluded by **exact title** as well as by age, which cannot race the clock that stamped it; `COALESCE(details->>'last_seen_at', created_at)` is the fallback, without which every row raised before this change would have been permanently unresolvable — the defect preserved by its own fix
  - **The obvious query would have OOMed on the backlog.** `_open_alerts` first read every unresolved row this agent owns, which on the live table is 593,814 ORM objects on the first run. It is now bounded by the titles the run is about to raise — single digits — which is the more natural query as well as the survivable one
  - **The double-ingest is fixed and the mechanism is a cursor, not a narrower window.** `read_journal` resumes from `__CURSOR`, so overlap is decided by journalctl rather than by a clock; narrowing `since` instead would have traded the duplicate for a gap whenever a run ran long, which is the worse failure for a monitor. Proven live: two back-to-back runs ingested **96 entries then 0**, where the old code re-read the whole window every poll. The cursor advances over **every** entry read, not only those surviving the severity filter — advancing only past kept entries leaves the resume point behind a run of info-level noise and rebuilds the defect one layer down. It is in memory, so a restart falls back to `_resume_floor`: the newest `logged_at` already stored for that source, passed as `--since @<epoch>` because journalctl reads a bare datetime as **local** time and everything here is UTC
  - **The `-n 500` cap is still there and is now reported.** A ceiling is unavoidable at 8.5 kernel messages a second; what was missing is that hitting it was invisible — `findings_count` sat at exactly 200 on every run and nobody read it as a symptom. `details['truncated_sources']` names the sources at their ceiling rather than counting them, because which one it is decides whether it matters. `-n` returns the *newest* entries, so a truncated read stays current and drops the middle, which is the right trade for a monitor and is now stated rather than inferred
  - **What this does not fix, deliberately**: the host fault. `BT_RAM_CODE_MT6639_2_1_hdr.bin` is still absent, the kernel retry loop is still running at ~8.5/s, and `rfkill list` now prints nothing at all — the adapter no longer registers a soft-block switch, so even the reversible stop recorded below is gone. The point of this change is that none of that matters to the alerts table any more: the storm costs 2 rows
  - **Symptom**: unresolved alerts under `agent='log_aggregator'`, counted live — `Log error: kernel` **547,814** (2026-03-24 → 2026-08-10), `Log critical: ollama.service` 29 for a service retired 2026-07-24, `Log error: alfred-backend.service` 19, plus five smaller titles. That is **91 % of every unresolved alert in the table**, and `GET /api/sysadmin/alerts` and the tray badge carry all of them
  - **Cause, and why it is not the one Session 41 just fixed**: `SNAG-AGENT-004` was a *state* alert with no observer of its recovery, and the fix was to ask which open alerts a run would not raise. That question is meaningless here. A log line that was written cannot un-write itself, so there is no run at which "this would not be raised" becomes true, and widening `_resolve_recovered` to cover these titles would resolve half a million rows on the first pass and then resolve every new one on the run after it raised it — an alert type with a lifetime of five minutes by construction
  - **The real defect is upstream of resolution**: `log_aggregator` raises **one alert row per matching log entry**. 547,814 rows for one recurring kernel message is not a backlog, it is a design that mistook a log for an incident. The same mistake the monitor made with `service_health` — *"that table records one row per failed check — 123 rows for one internet outage"* — recorded in `GET /api/services/reliability`'s docstring as the reason incidents are derived from transitions rather than counted from alerts
  - **Found**: 2026-08-12 while counting the population for `SNAG-AGENT-004`'s fix. Deliberately left out of that change: one mechanism resolving two different kinds of thing is how a row gets closed while still being true, which is the rule the three exclusions in `RESOLVABLE_TITLE_PATTERNS` exist to hold
  - **The volume is genuine, which was checked rather than assumed.** The obvious guess is that a poller with no cursor re-reads its own window; measured instead, `log_entries` holds 546,325 kernel events at **one** copy and 232 at two, so the rows are distinct events and not re-reads. **546,579 of the 547,814 (99.8 %) are two Bluetooth messages** — `Failed to load firmware file (-2)` and `Failed to set up firmware (-2)`, ENOENT — emitted in a retry loop **every ~0.58 s** across four days only (2026-07-15, -24, -27, -30; 175,000 rows on the worst). A missing firmware file produced half a million alert rows, and the alerting design turned a hardware annoyance into 91 % of the table
  - **The underlying host fault, run down 2026-08-12 and *not* what it looks like.** The working hypothesis was "the motherboard's wireless is unsupported by Linux". Half right, and the half that is wrong changes the fix. `modinfo btmtk` on kernel 7.1.6 declares `firmware: mediatek/mt7927/BT_RAM_CODE_MT6639_2_1_hdr.bin` — **the driver knows this exact chip and names the exact file**. `/lib/firmware/mediatek/mt7927/` (owned by `linux-firmware-mediatek 20260622-1`) contains only `WIFI_RAM_CODE_MT6639_2_1.bin.zst` and `WIFI_MT6639_PATCH_MCU_2_1_hdr.bin.zst`. The BT blob is on no path on this box, while **every sibling chip has one** (MT7902, MT7922, MT7961, MT7925). So it is a **firmware packaging gap, not a driver gap** — waiting on a newer kernel achieves nothing; the file is what is missing. *(Amended 2026-08-12 — see below: it is not a packaging gap either. The file does not exist upstream.)* The card is a MediaTek MT7927 (Filogic 380) at `09:00.0`, and the Wi-Fi half is the mirror image: firmware present, **no driver bound at all** (`lspci -k` prints no `Kernel driver in use`), which is the genuinely-unsupported part
  - **It came back the same afternoon, and disabling `bluetooth.service` did not stop it.** Measured 15:52 on 2026-08-12 with that service `inactive`: **816 Bluetooth kernel messages in two minutes (~6.8/s)**, twice the July rate, 81,422 this boot, the adapter having re-enumerated at 12:32:45. `bluetooth.service` is `bluetoothd`, **userspace**; the retry loop is `btusb`/`btmtk` in the **kernel**, which probes the adapter whether or not anything in userspace wants it — so the obvious lever is the wrong one. The kernel also now states the diagnosis itself: `Direct firmware load for mediatek/mt7927/BT_RAM_CODE_MT6639_2_1_hdr.bin failed with error -2`, character-for-character the file `modinfo btmtk` declares. **This promotes the entry from P2 to blocking**: the `SNAG-AGENT-004` fix does not touch these rows, so restarting the daemon while the storm runs refills the table Session 41 emptied, at 175,000–580,000 rows a day. `rfkill block bluetooth` is the reversible stop (`hci0` reads `Soft blocked: no`, so it is untried) and the firmware blob is the real fix *(both claims superseded — see the amendment below: `rfkill list` now prints nothing at all, and there is no blob to install)* — but both treat the symptom, and any recurring kernel error reproduces this until the raise rule changes
  - **Was dormant, which is why it read as harmless.** The last Bluetooth message of any kind is **2026-07-30 22:53**; there have been five boots since and `/sys/class/bluetooth/` does not exist, so the device is not enumerating at present. But `bluetooth.service` is still `enabled`, nothing was changed to stop it, and if the adapter reappears the loop resumes at ~3.4 lines/second — **~175,000 alert rows per day**. Guaranteeing it cannot recur means disabling the service (and optionally blacklisting `btusb`/`btmtk`); making it *work* means dropping `BT_RAM_CODE_MT6639_2_1_hdr.bin` from upstream linux-firmware into `/lib/firmware/mediatek/mt7927/`. **Neither is this repository's to decide**, and neither removes the defect above: one alert row per log line turns any recurring kernel error into a table like this one
  - **Amendment, 2026-08-12 (after the fix landed): the recorded host fix does not exist.** The entry above concluded that the blob was a packaging gap and that installing `BT_RAM_CODE_MT6639_2_1_hdr.bin` from upstream linux-firmware was the real fix. It is not available. Checked against the upstream tree and its manifest rather than inferred:

    ```
    git.kernel.org  linux-firmware  /mediatek/mt7927/
      WIFI_MT6639_PATCH_MCU_2_1_hdr.bin
      WIFI_RAM_CODE_MT6639_2_1.bin
    ```

    `WHENCE` declares those two files for the MT7927 and nothing else. **MediaTek has not published the Bluetooth firmware at all**, so this is a vendor *publication* gap, not a distribution one — `linux-firmware-mediatek 20260622-1` is current with upstream and `pacman -Ql` confirms it ships only the two WIFI blobs. The kernel driver was merged declaring a file its vendor has not released. Nothing to install, no newer package to wait for, and no way to date a fix
  - **Why the original reasoning failed, which is the part worth keeping.** "The driver names the exact file, every sibling chip has one, therefore the file is merely missing here" is sound and wrong. A `modinfo` firmware line is a **request**, not evidence of existence; sibling chips having blobs shows what the *packager* would ship if it existed and says nothing about whether the vendor published it. The one place that settles it — the upstream tree — was never checked, and checking it took one `curl`
  - **What actually stops it, and the cost is genuinely nil.** De-authorise the device so the USB core never configures it and nothing probes for firmware. It is at `0489:e13a` (Foxconn, the MT7927's Bluetooth half), on port `1-11`:

    ```
    # /etc/udev/rules.d/99-mt7927-bt-no-firmware.rules
    ACTION=="add", SUBSYSTEM=="usb", ATTR{idVendor}=="0489", \
      ATTR{idProduct}=="e13a", ATTR{authorized}="0"
    ```

    Bluetooth does not work either way, so nothing is lost; undoing it is one `rm` and a reboot. Blacklisting `btusb`/`btmtk` is the blunter fallback if the port still logs enumeration failures, and it costs any future USB Bluetooth dongle. **Not applied** — `sudo` on this box wants a password and the change belongs to the owner
  - **The two USB faults are one device.** `usb 1-11: device descriptor read/64, error -110` and `usb usb1-port11: unable to enumerate USB device` are port `1-11`, which is exactly where `0489:e13a` sits: the same adapter failing at an earlier stage of the same reset loop, not a second fault. The RCU stall used to argue for signature-keyed dedup is unrelated, so that argument stands, but two of the six "distinct faults" in the table above share a root cause
  - **What the storm still costs after the fix.** The alert table is bounded now — 2 rows, not 43,000 a day — but the lines are still ingested: `log_entries` holds **610,941 rows / 622 MB**, taking on **64,047 today**, against 30-day retention; journald is at **4 GB** with **165,670** kernel lines today. Not urgent, not free, and the reason the udev rule is still worth installing
  - **Recheck whether the blob has appeared** (there is no other trigger — nothing on this box watches for it):

    ```
    curl -s https://git.kernel.org/pub/scm/linux/kernel/git/firmware/\
    linux-firmware.git/tree/mediatek/mt7927 | grep -o 'BT_RAM_CODE[^"<]*'
    ```
  - **A second, smaller defect measured on the way**: `read_journal` is called with `since="2m ago"` on a **60-second** poll, so every unit-journal event is ingested **exactly twice** — confirmed at 2 copies for all 18 `venture-assistant-backend` and all 15 `sportsanalyser-frontend` events. Kernel does not show it and the reason is not established, so it is recorded as an open question rather than explained. Small in rows, but it doubles every count anything downstream computes from `log_entries`
  - **Fix, and the decision it is blocked on**: not a resolve rule but a *raise* rule. Options, none chosen: alert on a **burst** (n occurrences in a window) and resolve when the source goes quiet, which turns this into a genuine state; or drop alerting from the log aggregator entirely and let `log_entries` be the record, with `GET /api/logs/stats` the surface. The second is smaller and probably right — the tray has never been the place anyone reads kernel messages. Either way the existing rows are a data change needing an explicit call, and retention will not touch them while they are unresolved

- [P2] SNAG-ESTATE-002: **the estate's nudge title and message never reach the wire, so this repository built its own** (2026-08-13)
  - **Symptom**: `estate_service/projects/nudges.py` carries a comment reading "The one place a nudge's title is built" over `nudge_title`, and `Nudge.title` / `Nudge.message` are `@property`. `GET /api/projects/attention` serialises with `dataclasses.asdict`, which emits **fields only** — so neither property is in the payload. The producer's single source of truth for a nudge's wording is unreachable from the consumer that renders it
  - **Consequence, as landed**: `sysadmin/estate/judgements.py` builds `Project <name> next action idle` and the message text itself, from the published fields (`days`, `threshold`, `at_window_edge`, `next_action`). That is a second implementation of a format the producer believes it owns — the copy-drift the estate manager exists to remove, in the one place both repositories have a comment claiming otherwise
  - **Why it was accepted rather than fixed**: the title is the identity key in **this** repository's `alerts` table — dedup, the resolve and the tray's `{severity}:{title}` fingerprint all read it — so it is arguably ours to own regardless. What is not defensible is that both sides think they own it and neither says so
  - **The fix is the estate's, not ours**, per estate-manager ADR-0002: a cross-repo requirement is recorded there and executed there. Either publish `title` and `message` as fields (cheapest: convert the two properties, or add them in `oversight.attention` beside the `asdict` call), or delete the properties and the "one place" comment so the producer stops claiming a role it does not fill. Deciding which is theirs
  - **Handed over 2026-08-14**, recorded in estate-manager as its **`SNAG-ESTATE-010`** (snag_list.md) with a backlog task pointing at it. **The IDs do not correspond and must not be quoted interchangeably**: that repository already has a `SNAG-ESTATE-002` of its own — an estate rule cited by name and written nowhere, fixed 2026-08-11 — because the scheme is per-repository. Nothing was changed in its code, which is the whole point of the delegation
  - **The drift is not prospective; it has already happened.** `Nudge.message` puts the action through `_shorten` at `_MESSAGE_ACTION_CHARS = 120`, and `sysadmin/estate/judgements.py` interpolates `next_action` in full. The two messages differ **today**, and neither side can observe the difference — the producer's copy is never serialised and the consumer has never seen a populated payload. This upgrades the entry from a duplication that might drift to one that already has
  - **The strongest evidence for the delete option was not available when this was filed**: `nudge_title`, `NUDGE_TITLE_LIKE` and `NUDGE_TITLE_SUFFIX` have **no production caller anywhere in estate-manager** — the only reference outside their own module is one test. The comment claims "Raise, escalate and resolve all derive from it", and none of that machinery is in that repository at all; it stayed here at the Session 4 move. `TestTitleHasOneSource` pins a consistency with no consumer. So the producer is not merely failing to publish a format it owns, it is maintaining a format nothing on its side uses. The comment even justifies itself by citing `_alert_title` in `estate_service.projects.agent` — **a symbol that does not exist in that module**, having stayed here at the move, so the reasoning behind the claim left before the claim did
  - **A third property is dropped too** — `Nudge.details`, unmentioned when this was filed. Any fix that converts `title` and `message` and stops there leaves the same defect one field over
  - **Found**: 2026-08-13 by Session 45, by reading the producer rather than trusting its dataclass. Nothing catches it: `asdict` dropping a property is silent, and the estate's own suite asserts only `payload["nudges"] == []`
  - **The related gap this leaves here** is that `judge_attention` has never been exercised against a real populated payload — `/api/projects/attention` has answered `{"health": [], "nudges": []}` every time anyone has looked, on both sides of the seam. Its rules are pinned against literals built from the producer's dataclass fields, which is the strongest evidence available and is not the same as a capture
  - **Checked again 2026-08-14 07:30 UTC and still empty**, this time *after* an overnight `run_type: scheduled` scan (03:31 UTC, 26 projects, 0 parse failures) rather than the single manual scan that was all the estate had before. That was the precondition that made the check worth waiting for, so the negative result is worth more than the earlier ones: the payload is not empty merely because the producer had never really run
  - **The related gap is closed on this side (2026-08-16, Session 52); the cross-repo half stays delegated and open.** A populated payload cannot be captured from the wire — checked a fourth time, still `{"health": [], "nudges": []}` — so one was made from the producer's own code, driven read-only in its own venv against the live estate database with `effective_threshold` forced to 101 and `nudges.evaluate(default_days=0)`. Everything else is the estate's: 26 real snapshots, 26 real manifests, 5 real streaks, `dataclasses.asdict` over the real `Nudge`. It is committed as `tests/fixtures/estate_projects_attention.json`, and the seam is now guarded in `tests/test_estate_project_contracts.py` beside the two 8400 routes that already were — recorded half asserting the keys the consumer reads, live half asserting the envelope with the per-entry assertions **pre-staged** to start running the day the estate publishes anything
  - **Two defects the literals could not express**, both fixed: the family raised **31 rows in one poll** (26 breaches + 5 nudges, one tray fingerprint each) where the ports family has had `port_breach_max_rows` since Session 26b-A — now `attention_max_rows`, per family; and the message ran to **469 characters** into a notification body, now cut with `truncate_at_word` at 120 with the full text kept in `details`. Neither is the drift this entry is about, and the second **narrows** it: this repository now shortens the action with its own marked helper, so the two messages differ by wording and by a marker rather than by one side silently keeping 120 characters and the other 469
  - **A test now fires when the estate fixes its half.** `PRODUCER_DROPPED_NUDGE_KEYS` asserts `title`, `message` and `details` are *absent* from a published nudge, with a failure message naming the next move — take the producer's wording and delete the assertion. That is the pre-staged-trigger shape rather than a task parked on another repository
  - **One observation for the estate, not raised as a defect here**: `IdleNudgeConfig.days` has no lower bound (its per-project sibling `idle_nudge_days` is validated `>= 1`), so a global `days: 0` yields nudges reading "unchanged for 0 days (nudges after 0)" — the `standing_days: 0.0` artefact Session 26b-A found one surface over. Visible in the committed fixture because the capture forced exactly that; this repository renders the producer's numbers faithfully and does not launder them

- [P3] SNAG-ESTATE-003: **the estate-invariant alert families are raised once and then stay silent, with no escalation** (2026-08-13, **fixed 2026-08-16**)
  - **Symptom**: `EstateJudgeAgent` raises at `warning`, deduplicates on an open row, and never escalates. A scan that has been stale for six hours and a scan that has been stale for six days produce the same single `warning` row, spoken once
  - **This is exactly the failure Session 39 was written to fix**, one family over. That session's argument — *"a warning that fires once is indistinguishable from one that got fixed"* — applies unchanged here, and `sysadmin/core/escalation.py` is already in `core` with `Ladder` and `step_for`, reused by both `monitor/stalls.py` and (before the cutover) the project nudges
  - **Why it was left**, stated rather than forgotten. The loud rung is `critical`, which breaks through the DND windows by configuration and is the only severity the tray renders non-transient — reserved, by `stalls.py`'s own docstring, for a fault on **this box** that has already been announced and ignored. The estate being a day behind on a scan is not that. And the family most in need of escalation already has it *from the producer*: an idle nudge arrives pre-escalated on the estate's own ladder (`info` at 7 days, `warning` at 14), which this agent takes verbatim
  - **What would settle it**: a third rung, or a `warning`-that-repeats mechanism that is not `critical`. Neither exists, and inventing one to hold this family alone is the wrong shape — if a repeat-without-critical rung is right, `collation.py` and the service families want it too
  - **Found**: 2026-08-13 by Session 45, while deciding whether to wire `core/escalation.py` into the new agent
  - **Fixed 2026-08-16 (Session 53), and the entry's own "what would settle it" was answered in the negative first.** It proposed a third rung, and STATUS.md's recommendation sharpened that to *a third rung in `sysadmin/core/escalation.py` with three callers*. **That rung cannot be heard, and the measurement is the session's first deliverable.** `NotificationPolicy.fingerprint` is `{severity}:{title}` and `_FingerprintState.notified_this_episode` clears only when that pair is **absent from a poll** — which a resolve-and-re-raise inside one agent run never produces, because the tray sees only unresolved rows and both rows are unresolved-then-resolved between two polls. Driven against the real policy: a resolved row replaced by a fresh one carrying a **new message** produced **no notification at all**, where the same fault escalated to `critical` spoke, and a forked title spoke. So the two audible repeats are a severity change and a forked title — and four separate rules in this repository forbid the second, the title being the identity key for dedup, for the resolve and for the tray
  - **Therefore the repeat is a *notification* decision, not a lifecycle one**, and it went where notification policy already lives: `reminder_hours` in `sysadmin_tray/notifications.py`. That is the division this repository already states — *"the tray polls `GET /api/sysadmin/alerts` and owns the notification policy (dedup, flap cooldown, coalescing, digest)"* — and it fixes **every** deduplicating family at once rather than the estate's five surfaces alone, which matters because the estate family is the one place the defect has never actually fired: `estate_judge` has produced **two rows in its life**, both resolved. The live instance on 2026-08-16 was `service_discovery`'s `Unmonitored systemd units: 8 findings`, the **only** unresolved row on the box, open 24 hours and spoken once
  - **The interval is derived, not picked.** `reminder_hours: 24` matches `self_monitor.escalate_after_hours`, the only escalation gap configured on this box, so a family that *owns* a ladder escalates to a different fingerprint — a new episode, announced immediately — before any reminder of its quiet rung is due. Shorten it and the reminder becomes the first thing you hear twice, which demotes the loud rung from news to repetition. The reminder is thus what families with **no** ladder get, which is exactly the population the entry names
  - **Four rules, one of them the opposite of the first draft.** The clock runs from **when the tray last spoke**, not from `alert.created_at` — `stalls.py`'s rule (*the thing that failed was the telling*), and it keeps the single injected clock that makes every window in that module testable without sleeping. A reminder is **never transient**, because the failure it fixes is a toast in an empty room and `transient=False` is what keeps it in the notification history — `flush_digest`'s rule. The **fingerprint is unchanged**, so it updates in place and shares one snooze key: snoozing a fault silences its reminders without a second control. And **reminders fold separately from new alerts**, into `FP_REMINDER` with their own wording, because reporting a fault announced yesterday inside a summary headed "N new alerts" is the one thing a reminder is not — and `attention_max_rows` lets the estate judge put five rows on screen at once, so the volume is real
  - **A defect the fixtures could not have caught, found by a probe.** `state.first_notified_at or state.last_notified_at` reads a monotonic clock reading of exactly `0.0` as absent and falls back to the field every reminder resets — so each reminder reported the *interval* ("24 hours") rather than the age, for a fault that had stood three days. `FakeClock` starts at `1000.0` and every existing fixture with it. Fixed to `is None`, and `test_reminder_reports_the_episode_age_not_the_interval` starts its clock at zero on purpose
  - **Two limits stated rather than discovered later.** In `digest_mode` a non-critical is never reminded, because that mode's contract is that warnings never interrupt and a reminder is an interrupt — making the digest itself periodic is a separate question about a mode that is `false` here and has no live observations behind it. And the policy state is **in memory**, so a tray restart re-announces every open fault as new and restarts the cadence. The third limit is its own entry, `SNAG-TRAY-007`
  - **Fix**: `sysadmin_tray/notifications.py` (`reminder_hours`, `_reminder`, `FP_REMINDER`, `_coalesce` parameterised), `sysadmin_tray/config.py`, `tray_icon.py`, `app.py`, `config.yaml`, plus corrections to the two docstrings whose premise had moved — `sysadmin/estate/agent.py` and `sysadmin/core/escalation.py`, both of which said the omission was deliberate and now say why the alternative was refused. 10 tests in `tests/test_tray/test_notification_calm.py`, 2 in `test_config.py`. Suite **1802** green

- [P3] SNAG-TRAY-007: **the desktop understudy still speaks once — a standing fault repeats only while the tray is running** (2026-08-16, **fixed same day**)
  - **Symptom**: `sysadmin/monitor/desktop.py` is subscribed to `alert.raised` and speaks once per *incident*, staying silent while another unresolved row with that title is open. `SNAG-ESTATE-003`'s fix is `NotificationPolicy`'s, and the understudy shares none of it — so on a box where the tray is not running, which is the only circumstance the understudy exists for, a standing fault is still announced exactly once
  - **Why it is P3**: the tray is the primary speaker and runs here (`sysadmin-tray.service`, user scope). The understudy covers the window where it is not, and the defect it inherits is the pre-fix behaviour rather than a regression
  - **Why the fix is not a copy**: the understudy is **event-driven**, subscribed to `alert.raised` with no poll of its own, so there is no moment at which it could notice that a fault it announced six hours ago is still open. A reminder there needs a periodic read of the open rows — which is a scheduled job or a hook into an existing agent, i.e. a second owner of a lifecycle the tray already owns whenever it is up. That is the "second owner" defect this repository has now found at four scales, so it needs a decision about precedence (`tray_grace_seconds` already answers it for the raise path and would have to answer it for the repeat path too) rather than a patch
  - **Found**: 2026-08-16 by Session 53, while stating what the tray-side fix does not cover
  - **Fixed 2026-08-16 (Session 55)** by giving the understudy a clock of its own: `DesktopNotifier.sweep_reminders`, scheduled as `desktop_reminder_sweep` in `sysadmin/core/jobs.py`. The decision the entry asked for was taken in both halves it names
  - **The precedence answer is `tray_grace_seconds`, and it is the same window for both paths — but it is applied differently.** On the raise path the window *skips*: the tray is about to show this, so say nothing. On the repeat path a skip is wrong, because it leaves `last_spoken_at` sitting at the opening notification, and the first sweep after a tray outage then restates a fault the tray itself restated ten minutes earlier. So a watching tray **stamps the clock forward** instead — while something is polling the route, the last thing said was said by it. The entry's doubt ("the two answers are not obviously the same") was right: the window is the same and the *action* is not, and a test pins the middle window, where a skip and a stamp are the only thing that can be told apart
  - **The second-owner objection is answered by ownership, not by avoidance.** The sweep is a `JobSpec` — `core/jobs.py` owns *when*, `main.py`'s `JOB_TARGETS` owns *what*, and the notifier owns the lifecycle end to end. Hooking it onto the end of `SysAdminAgent._execute` was rejected: that makes an agent responsible for a lifecycle `monitor/desktop.py` holds, which is the defect this repository has now found at four scales and the reason the entry refused a patch
  - **The interval is derived twice over.** `reminder_hours: 24` is the tray's number for the tray's reason (it matches `self_monitor.escalate_after_hours`, so a laddered family reaches its loud rung as news before any reminder of its quiet rung is due) — and two speakers with *different* cadences would make the interval depend on which of them happened to be running, which is exactly what the understudy exists to hide. The sweep's own cadence has **no config leaf**: it is `max(60, tray_grace_seconds)`, because the sweep asks the two questions that window already answers and a second number beside it would be invented rather than derived. Floored so a grace window tuned to a few seconds cannot turn a derivation into a hot loop
  - **The population is what this process announced, never the open rows.** A sweep over `resolved IS false` adopts every fault the tray was speaking for and announces the lot on the first sweep after the tray dies — `SNAG-AGENT-005`'s unbounded `SELECT` wired to a notification each. The spoken set is in memory, so the query is `title IN (:titles_we_said)` and a sweep that has said nothing issues no query at all. What that costs is `SNAG-TRAY-008`, filed rather than left implied
  - **Two rules borrowed rather than rediscovered.** A roll-up takes the **loudest** rung it swallows and folds at two (Session 52's rule and the tray's `coalesce_threshold`) — `notify-send` has no `replaces_id`, so six due reminders would be six toasts. And a reminder that did not land does **not** move the clock, so it goes out on the sweep after the session bus returns rather than a full interval later; the same reason nothing is recorded as spoken until the opening notification has actually landed
  - **Verified live, because the unit tests mock every session and the `IN` clause had therefore never reached PostgreSQL** — Session 44's argument. Driven against the real `alerts` table: `_still_open` selected the live title and refused one never raised; the sweep restated once and then held; a synthetic row inserted, restated inside a roll-up of 2, resolved, and dropped from the spoken set; residue after rollback **0**. `apply_jobs` was driven against a real `BackgroundScheduler` and the real config.yaml — added at `interval[0:03:00]`, re-apply retimed nothing, widening the grace window to 600 retimed it to `interval[0:10:00]`, and `enabled: false` removed it
  - **Fix**: `sysadmin/monitor/desktop.py` (`_SpokenFault`, `sweep_reminders`, `_sweep`, `_still_open`, `_restate`, an injected clock), `sysadmin/core/jobs.py` (`desktop_reminder_sweep`, `MIN_REMINDER_SWEEP_SECONDS`), `sysadmin/core/config.py` (`reminder_hours`), `sysadmin/main.py`, `config.yaml`. 20 tests in `tests/test_desktop_notifier.py`. Suite **1854** green

- [P2] SNAG-DOCS-001: **`CLAUDE.md`'s Contract Registry documents fifteen project endpoints this service has not served since 2026-08-13** (2026-08-16, **fixed 2026-08-17**)
  - **Symptom, measured live 2026-08-16**: `GET /openapi.json` serves **one** route under `/api/projects` — `/api/projects/managed`. `CLAUDE.md` lists **fifteen** in its Contract Registry table, each with a contract model and an "Enforcement" column claiming `response_model`. `/api/projects/overview` and `/api/projects/board` both return `{"detail":"Not Found"}`. The directory `sysadmin/projects/` **does not exist** in this repository, and the file names five of its modules by path — `snapshots.py`, `momentum.py`, `nudges.py`, `branch_actions.py` and `next_action` — as the owners of rules it states in the present tense. `api/projects` appears 26 times
  - **Cause, and it is not a mistake anyone made twice**: the project domain moved to estate-manager on 2026-08-13 (ADR-0002's founding-extraction exception, closed the same day). The *estate* half of `CLAUDE.md` was updated and says so plainly; the **Contract Registry and the architectural narratives beneath it were not**, so the file simultaneously records that the domain left and describes it as present
  - **Why it is P2 rather than P3**: this is the file loaded into context at the start of **every** session in this repository. A stale roadmap document costs a session that reads it; a stale `CLAUDE.md` costs every session by default. It is the `SNAG-ROADMAP-002` failure — a document disagreeing with the live system — on the one surface where the disagreement is read before anything else
  - **What is *not* wrong, so the fix does not overshoot**: the narratives are good writing about real decisions, and several are cited by name from other entries and docstrings. `tests/test_estate_project_contracts.py` correctly remains, because this repository still *consumes* three 8400 project routes. The fix is to move each block behind a pointer to estate-manager the way the estate section already does — **not** to delete the reasoning, which is the estate's now and is worth reading there
  - **Found**: 2026-08-16 by Session 55, while reading the live state for the next-session ranking rather than trusting the documents' account of themselves — the rule those documents ask for, applied to the documents
  - **Fixed 2026-08-17 by Session 58.** The title's "fifteen" is wrong, and it is left standing because *how* it is wrong is the finding. The table held **twelve** `/api/projects` rows, and the population splits three ways rather than two: `GET /api/projects/managed` **is served here** (`sysadmin/monitor/routers/projects_managed.py` — ADR-0005 relocated it *within* this repository and kept its path, because its substance is live `service_health` wearing a project-shaped URL); `/overview` and `/{name}` are **consumed** from 8400, fetched by the tray and parsed with this repository's own models under `tests/test_estate_project_contracts.py`; only the remaining **nine** are neither. A blanket "point them all at estate-manager" — which is what the entry above literally asks for — would have deleted a live route's contract and relabelled a live seam as absent
  - **The narratives were nine blocks, not five.** Measured by grepping the tree rather than counting the paths this entry names: `snapshots.py`, `build_narrative_history`, `/next`, `momentum.py`, `nudges.py`, `/stale`, `status: archived`, the marker scan and `branch_actions.py`. Six further *sentences* compared a live thing to a departed one — "the file-organiser mirror of `/api/projects/actions`", "the third scorer, after the project organiser's repositories", "the two weekly reviews" (there is one), "both project sections read **one** snapshot query" (there are none) — and that is the shape a block-level sweep leaves behind, because each one reads correctly in isolation
  - **Two blocks were rewritten rather than pointed away, because the argument is still ours.** `SysAdminAgent._resolve_recovered` was written as the second half of "both scoring agents resolve alerts set-based": the project organiser made the argument, this repository still runs the statement, and pointing the whole block away would have left a borrowed rule looking invented. The escalation ladder's stated reason for living in `core` — `monitor` may not import `projects` — **expired with the domain**; it now has four climbers instead of two, so the placement outlived its reason, and the text says that rather than leaving a correct conclusion resting on a dead premise. That is exactly the trap `SNAG-AGENT-006` records one entry over, arriving in a document instead of a docstring
  - **Four things the entry did not name, found by sweeping rather than by reading it**: `sysadmin/registry/` is also gone (now `estate.registry` in `estate-lib`) and was still named as the manifest parser in the Database Configuration block, which is the block a new session reads before touching config; the ADR index listed only 0001 and 0002, so **ADR-0005 — the pointer target this entire fix points at — was not linked from `CLAUDE.md` at all**, and 0003 and 0004 were not either; the retention narrative justified `KEEP_LATEST_PER` with a route this service no longer serves, over two tables that are now frozen; and `tests/test_import_boundary.py` still forbids importing a package that cannot exist, which is a guard against bringing it back rather than a live constraint and is now described as one
  - **Verified against the box, not the tree.** `GET :8500/openapi.json` serves **one** route under `/api/projects`. Every claim about which contract models still have a caller was measured with a grep over `sysadmin/`, `sysadmin_tray/` and `tests/` with `contracts.py` itself excluded — the eight that came back zero are `SNAG-DOCS-002`, filed rather than deleted. `CLAUDE.md` is 1,684 lines from 1,774. Suite **1866** green: a documentation change cannot break it, which is the reason to run it rather than not to

- [P3] SNAG-ESTATE-010: **a judgement that gets *quieter* cannot reach a row that is already open** (2026-08-17)
  - **Symptom, live while it was found**: Session 57 shipped `TRANSIENT_HOLDER_SEVERITY = "info"` at 10:02 and the daemon restarted onto it at 10:06:38, so the fix is running. The two rows it was written for — `Estate port 3110 registry breach` and `Estate port 8110 registry breach`, both dev servers in `app-code-oss-26348.scope`, both listeners still bound — are still `warning`, with `details['holder']` **null**. They were raised at 2026-08-16 12:07, and `EstateJudgeAgent._execute` skips a judgement whose title is in `open_titles` before it looks at anything else, so neither the new severity nor the new `holder` blob reaches them
  - **The dedup is correct and is not what to change.** Raised-once-per-open-row is what stopped the pile-ups this file spends half its length on. What is missing is the *other* direction of Session 39's rule: escalation resolves the quiet row and raises a louder one, because the tray fingerprints on `{severity}:{title}` and an in-place change is never spoken. Nothing does the reverse, so **a fix that quietens a family is silent on every fault standing when it ships** — it applies only to breaches raised afterwards
  - **Why it is P3**: it self-clears. Close the editor, the listeners go, the sweep resolves both rows, and the next dev server is judged by the new code. The cost is bounded to the window in which a reclassified fault happens to be open — here, however long a VS Code window stays up — and to what the tray restates at `reminder_hours` in the meantime, which is `warning` for a fault Session 57 decided is `info`
  - **Not simply "resolve and re-raise on a severity mismatch"**, which is the obvious fix and rebuilds the flip-flop `monitor/collation.py` refuses: the judged severity is computed from `details` the estate republishes every hour, so a producer that wobbles between two rungs would clear and re-open the row each time, and every flip clears the tray fingerprint and notifies. Any fix needs a reason the new severity is *durable*, which the dedup deliberately does not have
  - **Found**: 2026-08-17 by Session 58, by looking at the two rows Session 57 was written for instead of at the code that was written for them — a documentation sitting checking whether yesterday's ship is visible on the box

- [P3] SNAG-DOCS-002: **eight project contract models survive with no reader, and four are re-exported to the tray** (2026-08-17)
  - **Symptom, measured 2026-08-17**: `sysadmin/core/contracts.py` still defines `StaleProjectsResponse`, `ProjectBoardResponse`, `NextProjectResponse`, `ProjectMomentumResponse`, `BranchCleanupResponse`, `PortfolioActionsResponse`, `ProjectRecommendationsResponse` and `ProjectReviewResponse`, with their members. A grep over `sysadmin/`, `sysadmin_tray/` and `tests/` excluding `contracts.py` itself finds **zero** readers of any of them. `sysadmin_tray/models.py` re-exports four, which is a name in an import list and not a caller
  - **Why it is the `SNAG-CFG-001` shape**: a pydantic model that validates nothing and is read by nobody is indistinguishable from one that is load-bearing. That entry was a config leaf parsed and read by nothing, and it looked exactly like the knob that decides whether an alert speaks. These are eight of them, in the file `CLAUDE.md` calls the contract registry
  - **Why it is P3 and not higher**: they cost nothing at runtime and they are not *wrong* — the routes they describe exist, on 8400, and three of the eight have a plausible future reader if the tray ever renders the estate's board directly. The risk runs the other way: someone reads `NextProjectResponse` here and concludes this service serves `/next`, which is `SNAG-DOCS-001` reconstructed from the code instead of the document
  - **Why it was not fixed in the same sitting**: removing a name from `sysadmin_tray/models.py` changes the tray's public surface, and Session 58 was a documentation sitting that touched no code. It also needs a decision this entry deliberately does not take — delete them, or keep them and give them the thing that would make them honest, which is a *consumer*: the seam test that already covers `/overview` and `/{name}` extended to whichever routes the tray intends to parse. Deleting is cheap and reversible; pre-staging the test is `pre-stage-blocked-cross-repo-work` applied to a decision rather than to another repository
  - **Found**: 2026-08-17 by Session 58 while fixing `SNAG-DOCS-001`, by measuring which of the registry table's rows still had a reader instead of deleting every row the live route table said was dead

- [P3] SNAG-TRAY-008: **the understudy forgets what it announced, and never adopts what it did not** (2026-08-16)
  - **Symptom**: `DesktopNotifier._spoken` is an in-memory dict, and the reminder sweep's population is exactly its keys. Two standing faults are therefore never restated by the daemon. (1) **A fault raised while the tray was up.** The understudy was correctly silent, so the title is not in `_spoken`; if the tray then dies, that fault is never restated — only faults raised *after* the tray went away are. (2) **Anything at all, after a daemon restart.** The set is rebuilt only by `alert.raised` events, so an open fault that was announced before the restart is silent until it happens to be re-raised
  - **Why it is P3**: it is the *narrowing* that makes `SNAG-TRAY-007`'s fix safe rather than an oversight in it. Widening the population to `resolved IS false` is the unbounded `SELECT` that made `SNAG-AGENT-005` fall over on the backlog it existed to end, and it would announce every standing fault at once on the first sweep after a tray outage. The tray carries the same in-memory limit for the same reason (`SNAG-ESTATE-003`'s second stated limit), so this is the understudy inheriting a known cost, not acquiring a new one
  - **Why it is not simply "persist the spoken set"**: a table would make the daemon's notification state durable and therefore *answerable to a retention policy*, and it would still not fix (1), which is a scoping question rather than a persistence one. The two faces have one root — the population is what this process said — so a fix that addresses only the second half leaves the first looking fixed
  - **Shape of a fix, so a later session does not re-derive it**: adopt a fault the daemon did **not** announce only when the tray has been absent for a full `reminder_hours`, and cap the adopted set the way `attention_max_rows` caps the estate judge's. That makes the first sweep after a tray outage quiet by construction and bounds the query without needing the spoken set to be complete
  - **Found**: 2026-08-16 by Session 55, as the stated cost of `SNAG-TRAY-007`'s rule 1 rather than by observation — the population is zero today

- [P3] SNAG-ESTATE-004: **the "never take a tool's default port" rule is written in the estate's own guide and enforced by nobody** (2026-08-14, **delegated**)
  - **Symptom**: `monitorable-project.md` §2.1 states the rule in prose — "8080 is llama.cpp's default… never take a tool's default port" — and annotates the one live violation by hand ("venture-assistant holds 8080 today only because it was allocated before this rule existed; it should move to 8301 when next touched"). Nothing checks it. The audit's `ports` check reconciles the table against `ss` in both directions and has no concept of a *contended* port, only an unclaimed or a silent one
  - **Consequence**: the rule degrades at the speed the guide is read. A hand-written annotation on one row is not a control — it says a violation was noticed once, not that the next one will be
  - **This was Session 26b's third checkbox and is being handed over rather than built here**, which is a change of owner and worth the reasoning. Detecting it is *filing a finding*, not alerting, so it is squarely the audit's remit (estate ADR-0003: conformance checks, never alerting and never fixing). It needs no privileges — the markdown table plus `ss -tln`, both already in `checks/ports.py`. And it is a rule about *their* document, in *their* guide, which the estate rules make theirs to enforce. Building it here would have this repository re-parse a cross-repo convention document, the same inversion that killed Session 26b's first checkbox
  - **Shape of the fix**, so the estate session does not re-derive it: a third branch in `run_check` beside `unclaimed_listener` and `claimed_but_silent`, over `WELL_KNOWN_DEFAULTS = {3000, 5000, 8080, 8888, 9000}` intersected with `claimed_ports`, at `SEVERITY_INFO` — **advisory, not `warn`**, because it has not collided and the remedy is a port move, which is work rather than a correction. Live population today: exactly one, 8080
  - **Not yet handed over.** Unlike `SNAG-ESTATE-002` this has no counterpart entry in estate-manager yet; it needs an estate-manager session to record it. **The IDs will not correspond** — that repository numbers its own `SNAG-ESTATE-*` series, which is how `SNAG-ESTATE-002` here became its `SNAG-ESTATE-010`
  - **Found**: 2026-08-14 by Session 26b-A, while checking which of Session 26b's four checkboxes the estate's 2026-08-13 audit had already overtaken

- [P2] SNAG-AGENT-006: **a sustained fault still writes one alert row per run — 60 rows for one dead timer in five hours** (2026-08-13, **fixed 2026-08-14**)
  - **Symptom, live while it was found**: `sysadmin-organiser-timer critical` held **60 unresolved rows** created between 07:41 and 12:36 on 2026-08-13 — one every five minutes, exactly `health_check_interval_seconds`. One fault, sixty rows. `venture-chat unreachable` shows 85 rows across 36 hours and `estate-broker-provision critical` 42, the same shape at whatever rate the fault recurred
  - **Cause**: `BaseAgent.raise_alert` inserts unconditionally, and `SysAdminAgent._handle_status` and `_check_thresholds` both call it on every run the condition holds. There is no open-row check on either path. `_resolve_recovered` protects by **title**, so the previous fifty-nine rows are protected alongside the newest one and none of them resolve until the fault clears
  - **This is SNAG-AGENT-004's other half, and naming it that way is the point.** That entry fixed the *resolve* side: 51,924 rows that could never be closed because recovery was observed inside the loop over configured services. It did not touch the *raise* side, which is what generated the volume in the first place — `redis unreachable` reached 6,283 rows one five-minute poll at a time. Session 41's fix means those rows now close and `run_retention` purges them at 30 days, so the leak is **bounded** where it used to be immortal. It is not gone
  - **What it actually costs now**, stated rather than assumed. The tray does *not* re-notify: `sysadmin_tray/notifications.py` fingerprints on `{severity}:{title}`, so sixty rows produce one toast. The cost is that `GET /api/sysadmin/alerts` and every count built on `resolved = false` read sixty times high, and a week-long outage would carry 2,016 unresolved rows for one fault. That is a legibility defect rather than a storm, which is why it is P2 and not P1
  - **Found**: 2026-08-13 by Session 44, while establishing whether the new collation check could safely follow the `_check_thresholds` pattern. It could not — a stale collation persists for weeks, so eight databases × 288 runs would be 2,304 rows a day — so `collation.py` dedups on an open row instead, and the divergence is what surfaced this
  - **The fix is not "add dedup to `raise_alert`", and that is the trap.** Dedup and `RESOLVABLE_TITLE_PATTERNS` are mutually exclusive: the sweep closes every owned row the run did not re-raise, which is sound *only* for a family that re-raises every run. Making the threshold and service families dedup without also removing them from that tuple makes their rows flip-flop — resolved on the run that holds, re-raised on the next — and each flip clears the tray fingerprint, so it starts notifying on every poll. A pile-up is loud; that would be loud *and* read as recovery. Whichever way it is fixed, both halves move together
  - **Left unfixed deliberately.** Session 44's subject was the collation check, and changing the lifecycle of the service and threshold families is the larger and riskier half of this — the families that carry `critical`, the ones the tray acts on. `sysadmin/monitor/collation.py` is the worked example of the pattern to copy, with the mutual exclusion documented against `RESOLVABLE_TITLE_PATTERNS` and pinned by `tests/test_collation_check.py`
  - **⚠️ The bullet above about the trap was superseded on 2026-08-14, and it is the part that would have produced a worse fix.** It says dedup and `RESOLVABLE_TITLE_PATTERNS` are mutually exclusive, so these families must be removed from the tuple. **That was true only of the exclusion set as it then stood** — it held the titles the run *raised*, so a family writing nothing on its second run had its still-true row swept. Removing the patterns would have undone SNAG-AGENT-004 and stranded every deconfigured service's rows again, since a set built from configuration cannot contain a service that has left it. **The patterns stayed.** Only the exclusion set changed, to the titles the run **judged** — the third option `sysadmin/estate/agent.py` had already proved and Session 45's handoff named
  - **Fixed 2026-08-14** by `SysAdminAgent._raise_judged`: it records the title in `_judged_titles` *first*, then asks whether a row is needed against `_open_titles`, one snapshot read at the top of `_execute`. Dedup suppresses the raise, never the judgement — so a fault that persists is in the protected set on every run and is never swept, and a fault that clears leaves it exactly once and resolves exactly once. `_raised_titles` and the `raise_alert` override that filled it are **deleted**: with the exclusion set no longer built from raises it had no reader, which is the SNAG-CFG-001 shape
  - **`% auto-restarted` is exempt, via an explicit `dedup=False`.** It is an event, not a state: `_failure_counts` resets to zero the moment `restart_unit` returns, so the title fires once per restart *cycle*, and a second restart three hours later is a second piece of news that dedup would swallow. It is still *judged*, so the sweep leaves it alone and it resolves with the rest of its family when the service is next measured healthy. Same distinction `log_signature.py` draws between a log line and an incident
  - **Suppression is deliberately not folded into `raise_alert`.** Five families — anomalies, stalls, agent failures, unit failures, collation — already own their own lifecycles, and changing all of them silently from one override is the "second owner of one lifecycle" defect at a sixth scale, which is the defect that produced this fix in the first place
  - **Every side effect had to be moved above the raise decision**, and `_threshold_keys` was the one that would have broken quietly: `_check_anomalies` reads it to suppress a duplicate anomaly for a resource that already has a threshold row, so a suppressed raise that also skipped the `add` would have fired an anomaly alert for a resource whose threshold row was open. The restart `systemctl` call, `_degraded_counts` and `_failure_counts` are all likewise unaffected by suppression
  - **The stated reason `collation` stays out of the sweep was itself wrong, and was corrected in passing.** The docstring said mutual exclusion; the actual reason is that collation resolves its own rows by id and contributes nothing to the judged set, so a sweep from here would be a second owner. The old sentence was a correct conclusion drawn from a premise that has now moved — the dangerous kind, because the next person removes the guard when the premise changes and reintroduces the bug the real reason was preventing
  - **Verified against the live database on 2026-08-14, in a transaction that was rolled back** — necessary because the unit tests stand in for PostgreSQL's `LIKE` with a Python matcher and so cannot prove that the patterns plus `NOT IN (judged)` select the right rows in real SQL. Run 1 wrote; run 2 with the same fault wrote nothing and resolved nothing (the flip-flop case); run 3 resolved on clearing; run 4 re-raised; ten sustained runs of one threshold fault left **1** row where the old code wrote 10; three auto-restarts left 3. Residue after rollback: 0
  - **Two changes of meaning, stated rather than discovered later.** `alerts_raised` in `agent_runs` now reads 0 on a run where a sustained fault is still true — that is its intended meaning (rows written), but anything trending it over time will show a step change at deploy, so `details['standing']` carries `judged` and `suppressed` beside it. And a `critical` that fires once now genuinely fires once: the volume that used to mask the absence of escalation is gone, which makes these families the same shape as `SNAG-ESTATE-003`

- [P3] SNAG-AGENT-007: **`_active_alerts` is now read four times per sysadmin run, and it loads whole ORM rows** (2026-08-14)
  - **Symptom**: `SysAdminAgent._active_alerts` issues `SELECT Alert WHERE agent='sysadmin' AND resolved IS false` and materialises full ORM objects. It is called by `_check_anomalies`, `_check_agent_health` and `_check_collation`, and since SNAG-AGENT-006 also once at the top of `_execute` for the dedup snapshot. Four unbounded reads of the alerts table per 300-second run
  - **Why it is P3 and not higher**: it was already three before this change, and the fourth caller is the one *shrinking* the table it reads. The query is also bounded in practice by the same fix — the row counts that made this dangerous (598,091 for the log family, 51,924 for services) are exactly what dedup and the set-based resolve now prevent
  - **Why it is worth filing anyway**: this is the `SNAG-AGENT-005` shape. `_open_alerts` there was first written as "every unresolved row this agent owns" and pulled **593,814 ORM objects** on its first live run — the fix falling over on the backlog it existed to end. Nothing structurally prevents a repeat here; what prevents it today is that the table is small, which is a state rather than a guarantee
  - **The fix is not free, which is why it was not taken mid-change**: the dedup caller needs only titles, so a `select(Alert.title)` projection would fix it — at the cost of a second definition of "this agent's open rows" sitting beside `_active_alerts`, which is the copy-drift `sysadmin/projects/snapshots.py` exists to argue against. The other three callers genuinely need the rows (id, severity, created_at, details) to run their ladders off, and must see what the run has already written, so they cannot share the `_execute` snapshot
  - **Found**: 2026-08-14 while fixing SNAG-AGENT-006, and deliberately left — splitting a shared query in the same sitting as a lifecycle change would have made both harder to verify

- [P2] SNAG-UNITS-004: **`GET /api/units/actions` emitted snippets that wire permanently-failing checks, and a removal command that leaves a timer behind** (2026-08-15, **fixed 2026-08-15**)
  - **Found by executing the advice, not by reading it.** Sessions 46, 47 and 26c made the diagnosis speak; Session 48 was the first to carry out what it says, and both defects surfaced inside an hour. One root cause: `recommendations.py` under-read a `UnitFinding` the sweep had already filled in
  - **Half 1 — the snippet never asked whether the unit is meant to run.** `grep -n "enabled" recommendations.py` returned *nothing*, while every finding carries a measured `enabled` that the orphan family has trusted since Session 46 for `armed`. Two of five `host` snippets named units that are **disabled and inactive** (`deadlock-api-ingest.service` system, and its updater timer). Pasting them declares `kind: systemd`/`kind: timer` checks — which assert *active* and *armed* — that return `critical` **every 300 s for ever**. Verified by running `_check_systemd`'s logic against both: `ActiveState=inactive → CRITICAL`. With `SNAG-AGENT-006` still open that is one row per run, the 12/hour that entry already measures as "60 for one dead timer in five hours". The pile-up Sessions 41–45 spent themselves deleting, arriving through this module's own remediation text
  - **The gate is "nothing enables it", not "it is not running"**, deliberately. `enabled` is an enablement symlink `scan.py` already walks and `classify_units` folds a oneshot's timer enablement into it, so a live schedule is never read as dormant — the sweep's no-subprocess promise survives, which it would not if the test were `ActiveState`. Measured 2026-08-15: the two `enabled=False` findings were both `inactive`, the three `enabled=True` all `active`, so the cheap signal and the sharp one agree on this box
  - **Half 2 — `removal_command` left a folded oneshot's timer installed.** `ticktick-sync.timer` declares `Requires=ticktick-sync.service`; the emitted command removed only the service. `monitor_unit` already named the timer, and the timer is the half carrying `[Install]` — so it holds the enablement symlink the command's own docstring exists to avoid orphaning. The next sweep cannot catch it either: a timer with no service is not a finding shape this module has. Now removed **timer first**, disarming the schedule before its service goes away
  - **The rule both halves imply**: an advice row offering no snippet must never say *"paste the snippet below"*. `sysadmin-failed.service` shipped exactly that — `snippet: ""` under paste instructions — an item an execution sitting **cannot close**, so it returns on every sweep for ever. `SNAG-ESTATE-001`'s roll-up defect wearing a single unit's name. Every no-snippet row now names its real next step; for a disabled unit that step is a fork (enable → next sweep emits a snippet, or remove)
  - **What the 20 failing tests were actually saying.** `UnitFinding.enabled` defaults to `False`, which is correct for `armed` — absent evidence must read as "not armed", the quiet direction — and the **opposite polarity** from this gate, where absent evidence suppresses advice, the loud direction. One field, two consumers, opposite safe defaults. Fixed in the fixtures, not the default: flipping the default would quietly arm every orphan
  - **Fix**: `sysadmin/units/recommendations.py` (`_snippet_for` gate, `_no_snippet_action`, `_enable_command`, `removal_command`), 7 tests in `tests/test_unit_recommendations.py`. Suite 1708 green

- [P2] SNAG-UNITS-005: **five system-scope orphans and a pending restart are blocked on `sudo`, which no agent session can supply** (2026-08-15, **durable half fixed 2026-08-15; ops half done by the owner and verified 2026-08-17 — closed**)
  - `offline-agents-dashboard`, `personalassistant-backend`, `personalassistant-frontend`, `ticktick-sync` (+ its timer) and `ticktick-sync-db` are all confirmed dead — the first, fourth and fifth point at directories that no longer exist; the two PersonalAssistant units were confirmed retired by the owner this sitting, `/opt/personalassistant` being leftover payload rather than a live project
  - The user-scope orphan (`garmin-sync.service`) **was** removed, so the emitted command is proven correct as written; only the privilege is missing. All six unit files are backed up
  - **The same wall blocks the deploy**: `services.yaml` is read at start-up, so Session 48's three new host entries are inert until `sudo systemctl restart sysadmin.service`. This is the second sitting in a row to hand a restart forward — worth a decision about whether the daemon should reload `services.yaml` on `SIGHUP`, which would remove the class of blocker rather than the instance
  - **Not a defect in the advice**: the emitted commands correctly say `sudo`, and refusing to run them unprivileged is right. It is filed because a recommendation nobody on the box can execute is indistinguishable, on the endpoint, from one nobody has got to
  - **The durable half is fixed (Session 49): `sysadmin/reload.py`**, reached by `SIGHUP` or `POST /api/sysadmin/reload`. The entry called this "worth a decision about whether the daemon should reload `services.yaml` on `SIGHUP`"; it was taken, and widened on measurement — every agent already calls `get_config()` inside `_execute`, so config.yaml's thresholds were re-read per run all along and only the scheduler's triggers, the engine, the socket and the logging setup are genuinely read once
  - **The privilege claim the entry rests on was checked rather than assumed.** `sysadmin.service` is a *system* unit but runs `User=gaddi`, so the owner may signal it without `sudo` — probed with `kill -0`, which tests permission without delivering. `systemctl reload` would need an `ExecReload=` line and *that* edit does need `sudo`, so the raw signal is the half that removes the blocker. Python's default `SIGHUP` action **terminates**, so a HUP sent to a daemon predating the handler is a restart wearing a reload's name, and the handler is asserted by a test rather than assumed
  - **Verified against the exact blocked instance**: with Session 48's three new entries removed to stand in for the running daemon, a reload of the real `services.yaml` reports them `added`, installs all three, and reports `requires_restart: []`. Had this existed on 2026-08-15 the restart would not have been owed
  - **The five orphan removals are untouched and still owed** — those are `systemctl disable` and `rm` under `/etc/systemd/system`, which no reload path can reach. The entry conflated a *deploy* blocker with an *ops* blocker; only the first is removed, and the second is not a defect in anything this repository ships
  - **Both halves are now done, and neither closure was recorded when it happened.** Verified 2026-08-17: `offline-agents-dashboard`, `personalassistant-backend`, `personalassistant-frontend`, `ticktick-sync` (+ timer) and `ticktick-sync-db` are **absent** from `/etc/systemd/system` and all report `not-found`; the restart landed **2026-08-16 12:06:09** (PID 1410826 → 1914354, `POST /api/sysadmin/reload` now 200). The bullet above went on saying "untouched and still owed" throughout, which is `SNAG-ESTATE-008` — this entry is one of its three instances, and the only one whose evidence was a single `systemctl is-enabled` call away

- [P3] SNAG-UNITS-001: **the unit sweep's snippet always says `kind: systemd`, so following its advice under-monitors every HTTP service** (2026-08-14, **fixed 2026-08-15**)
  - **Symptom**: `_services_yaml_snippet` emits `kind: systemd` for every non-timer unit it advises on, in both the `unmonitored` and `host` categories. A `kind: systemd` check asserts only that the unit is *active*. A backend that is running while every request 500s is active, healthy by this check, and broken — which is the failure an HTTP service is most likely to have and the only one a unit check structurally cannot see
  - **It is documented rather than accidental**: the docstring says *"`kind: systemd` rather than `http`: this scan does not know the unit's port"*, which is true and is the right floor for a **generic** generator. The scan reads unit files; a port lives in an `ExecStart` argument, a config file, an environment file or nowhere at all. Inventing one would produce advice that fails at load
  - **Why it is still a defect**: the advice is presented as a paste-ready snippet with no marker saying it is the weaker of two shapes. Nobody pasting it is told that a port would buy them a real check, so the sweep quietly converts every future HTTP service on this box into a liveness-only one. That is the `SNAG-SYSD-002` shape — a check that records less than the reader believes
  - **Found**: 2026-08-14 while pre-staging the SearXNG entry, which is the case that makes it concrete: SearXNG's port *will* be knowable (estate-manager's registry claims it before anything listens), so the generated snippet would be strictly worse than the hand-written one for a service whose port is a matter of record
  - **The fix is a comment, not a parser**: emit the `kind: systemd` line with `# no port known — use kind: http with a url if this serves one`, rather than attempting port extraction. Left unfixed here because the SearXNG session pre-staged its own entry and `tests/test_searxng_wiring.py` pins the shape for that one service; the general case needs its own sitting and a decision about whether `LISTEN`/`ExecStart` sniffing is worth the false positives
  - **Fixed 2026-08-15 (Session 26c), and the entry's own premise is what changed.** It reasoned that the comment was the only honest fix *because* "this scan does not know the unit's port" — true of the sweep, and no longer true of the sweep's siblings. `sysadmin/units/ports.py` reads `/proc/<pid>/cgroup` for every listener, so the snippet now emits `kind: http` with `url:` and `port:` whenever the unit holds **exactly one** port in the registry's audited range. Neither `LISTEN` nor `ExecStart` sniffing was needed, which is the alternative the entry was right to hesitate over — both would have guessed where the kernel can be asked
  - **The comment survived the fix rather than being replaced by it.** Where no port is attributed — nothing listening, a root-owned or containerised socket `ss` will not name, or two ports where picking one is a guess — the entry still says `kind: systemd` and still carries the marker, because the person pasting it is the one who knows what the unit serves
  - **What the fix does *not* buy, measured rather than assumed: it has an empty population today.** All 12 units holding an audited port on this box are `monitored`, and `classify_units` drops those before they become findings — the same two-thirds-invisible shape `SNAG-UNITS-002` hit. Driven as a counterfactual (pretending `alfred-backend.service` were unwired), the generated entry reproduces the hand-written one exactly: `url: http://localhost:8100/api/health`, `port: 8100`. So this is advice for the *next* service someone wires up, which is what the tier is for, and SearXNG was exactly that case
  - **The health path is still a guess, and it is now a snag of its own** (`SNAG-UNITS-003`). The sweep observes a port and never fetches

- [P2] SNAG-UNITS-002: **15 of the 18 units on this box that restart cannot ever reach `failed`, so no `OnFailure=` can fire for any of them** (2026-08-14, **fixed 2026-08-15**)
  - **Symptom, measured**: of 18 hand-written units declaring a `Restart=` policy, only `sysadmin.service`, `alfred-backend.service` and `alfred-frontend.service` have a start limit their restart cadence can actually reach. The other 15 include every live service on the box — `venture-assistant-backend`, `alfred-inference`, `estate-manager-api`, `venture-chat`, `venture-chat-large`, `venture-embed`, `sysadmin-tray`, both `deadlock-api-ingest` units, both `sportsanalyser` units
  - **Cause**: the failure is arithmetic, not an omission. Systemd's defaults are `StartLimitIntervalSec=10s` / `StartLimitBurst=5`, and every one of these units sets `RestartSec` to 5, 10 or 60 seconds. Five starts ten seconds apart span 40 seconds and the window is 10, so the limiter never fires, the unit stays in `activating (auto-restart)` for ever, `systemctl is-failed` reports nothing wrong, and any `OnFailure=` hook is unreachable
  - **This is the estate-wide form of `SNAG-ESTATE-001`.** That snag's two PersonalAssistant units restart-looped 52,178 times and stalled the kernel in exactly this state. Session 39 fixed the shape on `sysadmin.service` and the estate has no rule requiring the pairing on anything else — this is the first time the population has been counted rather than asserted
  - **Re-measured 2026-08-14 while wiring SearXNG: the population is now 17 of 20, not 15 of 18, and it grew the same day it was filed.** The two additions are `estate-manager-searxng-shim.service` and `estate-manager-searxng.service`, both `Restart=on-failure` with `RestartSec=5`/`10` against systemd's default 10-second window — the deploy did not do anything unusual, which is the point: **the defect is what a correctly-written unit gets by default on this box**, so the count rises with every service the estate adds. Filing it as a fixed list of 15 understates it as a standing rule. One mitigation worth recording rather than assuming: the shim is now checked `kind: http`, so a shim wedged in `activating (auto-restart)` is caught as `unreachable` by this service even though it can never reach `failed` — monitoring is the safety net for the units the ladder cannot see, which is an argument for the sweep's advice, not against this snag
  - **Found**: 2026-08-14, by `sysadmin/units/scan.py:restart_is_bounded` being written for SNAG-ESTATE-001's armed-orphan family and then pointed at the whole sweep
  - **Deliberately not alerted on**: the armed-orphan family raises one row per unit, and 15 rows on the first run is the pile-up shape wearing a new hat — it would train the reader to dismiss the family before it had said anything true. Two candidate fixes, neither costed: report it as a single rolled-up finding beside the existing one (cheap, and the roll-up is the surface SNAG-ESTATE-001 says nobody reads), or emit it as ranked advice under `GET /api/units/actions` with the `StartLimitIntervalSec=` line to paste (matches the tier's advice-only design, and the fix is genuinely one line per unit). The second needs a decision about whether a unit **this repository does not own** should be advised on at all
  - **What it does not claim**: the arithmetic assumes systemd's documented manager defaults, hard-coded in `scan.py` rather than read from `systemctl show` to keep that module pure. Confirmed against this box on 2026-08-14; a host overriding them in `system.conf` would make the check optimistic, which is the safe direction
  - **Fixed 2026-08-15 (Session 47) as advice, not alarm**, taking the entry's second candidate — a `restart` kind under `GET /api/units/actions` ranked second, above `unmonitored`, with the `StartLimitIntervalSec=` lines to paste. The open decision ("should a unit this repository does not own be advised on at all") was put to the owner and answered **yes, all of them**: the tier already advises on units it does not own — every `unmonitored`/`host` recommendation tells you to wire another project's unit — and the estate rule that bites is about *writing* into another repository, not about serving a pointer over a GET. The recommendation names the owning project so the reader knows whose edit it is
  - **The entry costed the fix as "one line per unit" and that was the part that was wrong.** Measured before writing anything: **11 of the 17 are `monitored`**, and `classify_units` drops monitored units before they become findings, so two thirds of the population had nothing to hang a line on. The fix needed a second pass over the same units (`restart_risk_findings`) — deliberately **not** a fifth category, because `units_scanned` is the sum of the four buckets and a monitored unit appearing twice would break the one arithmetic property `UnitScanSummary` exists to make auditable. It takes the shape `armed` already takes: a subset reported beside the sum, never inside it
  - **Orphans are excluded, which is the opposite of the obvious rule and shrinks the family to 13.** A broken unit that also loops reads like the worst case and belongs here twice over. It cannot: the orphan recommendation is *remove it*, and a start limit on a file you should delete is two contradictory instructions for one unit. Nothing is lost — an armed orphan that loops is exactly what `armed_alert_severity` already promotes to `critical`. So the live family is 11 monitored + 2 host, not 17
  - **No alert family, and the count is deliberately outside `actionable`.** `scan.actionable` is the roll-up alert's title *and* the number `alert_threshold` is compared against, so 13 latent risks added there would trip the threshold on their own and would read as 13 new units to wire up — two unrelated things behind one number. It rides in `details['restart_unbounded']` as evidence. The entry's first candidate (a rolled-up finding beside the existing one) was rejected for the reason it half-states itself: the roll-up is the surface `SNAG-ESTATE-001` proved nobody reads
  - **The remedy is checked against the detection, which is the trap this family was closest to.** `suggested_start_limit_interval` picks the smallest round window clearing `RestartSec x (burst - 1)` by 1.5x, and a test feeds every live shape back through `restart_is_bounded` requiring `True`; a second test appends the emitted snippet to a real unit file and re-scans, which is the only thing proving the two lines land in a section systemd reads them from. Advice that clears a symptom without fixing the fault is exactly what `ALTER DATABASE … REFRESH COLLATION VERSION` does in `SNAG-DB-002`, and it is cheap to build here by accident. Where **no** window would help — `RestartSec` beyond an hour, or `infinity` — it emits no snippet at all and says to lower `RestartSec` instead, because a number that does not work costs the family its reader
  - **The margin cuts the opposite way from the intuition, and the code says so.** A *longer* `StartLimitIntervalSec` is a *stricter* limiter: widening the window lets more starts fall inside it. Anyone "tightening" it to be safe would make the loop more immortal, which is why the direction has its own test
  - **`UnitFinding` now carries `restart_sec`, `start_limit_interval` and `start_limit_burst`.** Without them `restart_bounded` asks the reader to trust arithmetic they cannot see, and the naive version of that arithmetic is wrong in **both** directions — which is the entry's own best point, previously true only inside `restart_is_bounded`'s docstring
  - **What is still not fixed**: nothing re-checks a unit after the snippet is pasted, so the family clears on the next 6-hourly sweep and there is no "you fixed 3 of 13" signal; and the count will keep rising with every service the estate adds, because the defect remains what a correctly-written unit gets by default on this box. Neither is a defect in the fix — the first is the tier's advice-only design and the second is the standing rule this entry identified

- [P3] SNAG-UNITS-003: **the generated `kind: http` url guesses the health path, and on this box that guess is wrong more often than right** (2026-08-15)
  - **Symptom**: `_services_yaml_snippet` emits `url: http://localhost:<port>/api/health` from an observed port. The path is the contract's (`monitorable-project.md` §2.2 requires `GET /api/health`), not an observation — the sweep reads sockets and never sends a request
  - **Measured against every hand-written entry rather than sampled**: of the 11 `services.yaml` entries declaring a port and a unit, `/api/health` is correct for **4** (`alfred`, `venture-assistant`, `estate-manager-api`, `searxng`) and wrong for **7** — `llama-server`, `venture-chat` and `venture-embed` on `/health`, `sports_analyser` on `/api/v1/health`, `sysadmin-service` itself on `/health`, and both frontends with no path at all. The first draft of this entry said "two of twelve", from the two cases that happened to be in front of me; counting the file gave the opposite conclusion
  - **Kept anyway, and the reasoning is the entry's real content.** A wrong url fails **loudly** — the service reports `unreachable` within one 300 s poll of the reader pasting and restarting, and the fix is one line. `kind: systemd` under-monitors **silently and for ever**, which is `SNAG-UNITS-001` itself. Trading a silent wrong answer for a loud one is the same trade `schema_guard` makes by refusing to boot, and the same one `SNAG-DB-002` records in reverse (`REFRESH COLLATION VERSION` turning a loud known risk into a silent one)
  - **Mitigated rather than hidden**: the snippet carries two comment lines — that the port is observed and the path is the contract's default, and that `/health` and `/api/v1/health` are both in use here — so the fix is an edit rather than an investigation. A test asserts the marker is present
  - **Why it was not fixed here**: knowing the path needs a request, and a request belongs to the sysadmin agent's `http` check — which already makes one every 300 s — not to a six-hourly sweep whose no-network character is what lets it be tested against a directory under `tmp_path`. The candidate fix is to probe `/api/health`, `/health` and `/api/v1/health` once when a snippet is generated, and emit the one that answers; it needs a decision about whether the sweep may make outbound requests at all
  - **Found**: 2026-08-15, by running the new snippet generator against the live box as a counterfactual rather than only against fixtures

- [P3] SNAG-ESTATE-005: **the estate's port registry gives 8500 to `sysadmin-service`, which is not a project id on this box** (2026-08-15)
  - **Symptom**: `monitorable-project.md` §2.1 row for 8500 names `sysadmin-service`. The manifest id is `sysadmin-assistant` and the directory is `sysadmin_assistant`, so neither matches. Session 26c's `wrong_project` check finds the unit holding 8500 (`sysadmin.service`) belongs to `sysadmin_assistant` and the row claims something else
  - **Reported as evidence, not as a finding**, and deliberately: a registry row may legitimately name a third-party daemon — `_syncthing_` claims 8384 and is not a repository — and telling a typo from a daemon needs judgement this check does not have. "Wrong project" and "not a project" are different faults and only the first is ours. It lands in `findings['ports']['unknown_registry_projects']`
  - **This is the estate's document and the estate's fix**, per rule 2: the audit files findings about a repository, the repository fixes them under its own process, and neither writes into the other. Recorded here so it is not lost, to be raised with estate-manager as a recommendation rather than a ruling
  - **Found**: 2026-08-15, on the first live run of `judge_ports` — the only thing it had to say about a box where nothing collides

- [P3] SNAG-ESTATE-006: **the audit publishes a finding's `code` nowhere, so the judge's `details['code']` is `None` on every payload the estate can serve** (2026-08-16, **delegated**)
  - **Symptom**: `judge_audit_findings` writes `details['code'] = finding.get('code')` and its rule 4 says "the code lives in `details`". It does not. `GET :8400/api/audit/findings` publishes eleven keys per finding and `code` is not among them — verified against the live route and against a payload the producer built with a real `unclaimed_listener` breach in it
  - **Cause**: `estate_service.audit.finding.Finding` has a real `code` field, uses it as the last segment of `fingerprint` (`<check>:<subject>:<code>`), and then loses it at storage — `AuditFinding` has no `code` column, so `_record` cannot write one and the route cannot read one. It is `SNAG-ESTATE-002`'s shape exactly: a value the producer computes and the wire discards, except that this one is dropped by a **missing column** rather than by `asdict` skipping a property
  - **Cost today: none**, stated so it is not over-read. `unclaimed_listener` is the only ports breach the check emits, so nothing is ambiguous, and `details['fingerprint']` carries the code where a human can read it. What it costs is the docstring's claim, which is now corrected
  - **Not worked around here, deliberately.** The only workaround available is splitting `fingerprint` on its last colon, which is this repository parsing an identity format the estate owns — `_port_of`'s rule 3 one field over ("a finding whose port will not parse is skipped rather than titled from the sentence"). `judge_audit_findings` goes on reading `code`, so the day the producer publishes one it starts working with no change here
  - **Pre-staged rather than parked**: `tests/test_estate_surface_payloads.py::test_the_producers_code_never_reaches_the_wire` asserts the **absence** and fails, with the next move in its message, on the day estate-manager adds the column. The `PRODUCER_DROPPED_NUDGE_KEYS` shape, which is where this repository put the same problem in Session 52
  - **The IDs will not correspond** — estate-manager numbers its own `SNAG-ESTATE-*` series, the way this repository's `SNAG-ESTATE-002` became its `SNAG-ESTATE-010`. Not yet handed over; it needs an estate-manager session to record it
  - **Found**: 2026-08-16 by Session 54, driving the producer's `ports.run_check` against real listeners on 3900–3905 and storing the findings through its own models

- [P3] SNAG-ESTATE-007: **the queue surface stamps its timestamps in local time while every other estate surface stamps UTC** (2026-08-16, **delegated**)
  - **Symptom**: a granted lease comes back as `"granted_at": "2026-08-16T09:31:01+01:00"`, where `/api/projects/invariants`, `/api/audit/invariants` and `/api/audit/findings` all serve `+00:00`. Same process, same database, two renderings
  - **Cause**: `estate_service/projects/db.py` builds its SQLAlchemy engine with `connect_args={"options": "-c timezone=utc"}` and says why — the Session 4 cutover diff caught asyncpg and psycopg disagreeing about the wire format. `estate_service/db.py`'s `AsyncConnectionPool`, which the arbiter uses, passes only `row_factory` — so `psycopg` renders `timestamptz` in the connection's timezone, which is the box's `Europe/London`
  - **Costs nothing here and is filed anyway.** `judge_queue_invariants` carries `active_lease` into `details` verbatim and parses none of it, so no judgement is wrong. The cost is to whoever parses it next and assumes the estate speaks UTC, which three of its four surfaces do and which the surviving comment in the *other* engine explicitly promises
  - **Observed rather than reasoned about**: the live queue has never had a granted lease at the moment anyone looked (`"active_lease": null` on every reading), so this came out of driving `Arbiter.invariants()` against synthetic leases on a connection built with the producer's own pool kwargs
  - **The estate's fix, not this repository's** — one `kwargs` entry in their pool, and rule 3 forbids writing code into another repository whatever the size

- [P2] SNAG-ESTATE-008: **nothing checks a documented ops action against the box, and all three in the block that opens every sitting were already done** (2026-08-16, **scope corrected the same sitting** — filed for one stale action, measured at three of three)
  - **Symptom**: `SNAG-DB-002`'s `REINDEX` half was carried out by estate-manager on 2026-08-13 between 18:03 and 20:29. `snag_list.md`, `tasks.md`, `STATUS.md` and `HANDOFF.md` each independently restated it as pending until 2026-08-16 — **three days and five sittings** — with `STATUS.md` carrying it as a standing **sub-session action**, which is the block the global rules require to be read first at the top of every sitting
  - **This application observed the remedy land and said so.** `monitor/collation.py` saw the estate's first pass on the next five-minute poll and resolved all eight rows at `18:01:48`; `details->'collation'` has read `mismatched: 0` on every run since. The observation was correct, timely and machine-readable, and nothing read it back — `SNAG-ESTATE-001`'s roll-up defect with the polarity reversed, which is the worse direction. An unread fault is a missed alarm; an **unread recovery** is an instruction to redo finished work, and the work in question was a `REINDEX` of a live 16 GB database
  - **Cause is structural rather than an oversight.** Two repositories own a collation check deliberately — this one alerts, the estate files findings — and nothing reconciles the two. `EstateJudgeAgent` reads `/api/audit/findings` and is narrowed to `check == "ports"` (Session 26b-A), on the stated grounds that every other check is either this service's own alerts arriving through a second producer or another repository's conformance. **Collation is the case that narrowing did not anticipate**: shared infrastructure, owned by neither application, remedied by the estate — so the estate's record is the only record that the work happened, and it sits in the one finding class this repository declines to read
  - **Three candidate fixes, none costed, and the cheap one is not the obvious one.** (1) Widen the judge to `check == "collation"` — re-imports this service's own alerts through a second producer, which is precisely what rule 3 exists to prevent, and gives one fault two owners. (2) Have a document check compare a closed ops action against the estate's audit — a new consumer of a surface that may 500, and a new way for a document to be wrong. (3) **Give this repository's own resolve a reader**: nothing anywhere treats "an ops action in a document with no open alert row behind it" as suspect, and that is the signal this case already generated correctly. The third adds no cross-repo dependency and is the one to price first
  - **The narrow half is cheap and separable, and would have caught this instance**: `scripts/claude-preflight.sh` prints the handoff and the priorities without checking either against `sysadmin.alerts`. A single query — ops actions named in `STATUS.md`'s sub-session block against open rows — is not a fix for the general case and is one line of `psql` in a script that already runs every sitting
  - **Filed for one item and immediately measured at three, which is the entry's real finding.** `STATUS.md`'s sub-session block and `HANDOFF.md`'s "Blocked / owed" list carried three actions at the close of Session 55. **Every one had been carried out**, and none of the three by the party that recorded it:
    1. **`sudo systemctl restart sysadmin.service`** — done **2026-08-16 12:06:09**, seven minutes after Session 55's own commit (`a330e31`, 11:59). PID 1410826 → **1914354**; `POST /api/sysadmin/reload` returns **200** where the block says it 404s; and `desktop_reminder_sweep` is scheduled in the running process at `interval[0:03:00]`, which is the one thing the block said was *entirely* invisible until the restart landed. The block was written after the restart it asks for
    2. **The five system-scope orphan removals** (`SNAG-UNITS-005`) — `offline-agents-dashboard`, `personalassistant-backend`, `personalassistant-frontend`, `ticktick-sync` (+ timer) and `ticktick-sync-db` are all **absent** from `/etc/systemd/system` and all report `not-found`. Done by the owner
    3. **`SNAG-DB-002`'s `REINDEX`-before-`REFRESH` remedy** — done 2026-08-13 by estate-manager, above
  - **That reframes the fault from a cross-repo seam to a missing check**, and widens it in the direction that matters. One stale action is an oversight; **three of three** is the block having no verification at all, and two of the three are plain `systemctl` state this repository already reads every 300 seconds. The original framing — "estate-manager's remedy is invisible here" — was true and was the *narrowest* reading available, arrived at because the first item measured was the cross-repo one
  - **Each of the three had its evidence somewhere on the box, in a different place**: an `alerts` row this application resolved itself (collation), `systemctl is-enabled` reporting `not-found` (the orphans), and the daemon's own `MainPID` plus a route that had stopped 404ing (the restart). No single query covers them, which is why candidate 3 above is better read as a **convention** — every ops action names the check that closes it — than as one clever query
  - **`STATUS.md` is now the first version of that block to be empty**, and an empty block is itself a claim needing the same treatment: "nothing owed" and "nothing checked" must not render identically. `UnitScanResponse.ports_checked`'s rule, arriving in a document rather than a payload
  - **P2 stands, and the reasoning changed.** Filed at P2 because re-running a completed remedy would have been a 30-second idempotent no-op — the estate's script skips a database already at the current version; kept at P2 for a different reason — the cost is not a wrong action but five sittings of ranking attention spent on settled items at the top of the document that sets the agenda, plus a **restart the owner may have performed because the document asked for it after it was already done**
  - **Found**: 2026-08-16, by taking `SNAG-DB-002` as a session and measuring the box before reading the entry — the global rule "read the live state, not what the documents claim about themselves", failing on the one snag whose whole subject is a live-state query, then widened within the hour by checking the other two items in the same block rather than stopping at the one the session came in for

- [P2] SNAG-DB-002: **every PostgreSQL database on this box has a stale collation version**, and all three warn on every connection (2026-08-11, check half **fixed 2026-08-13**, remedy half **carried out by estate-manager the same evening** and **verified here 2026-08-16**)
  - **Symptom**: `psql -d projects` prints `WARNING: database "projects" has a collation version mismatch — created using collation version 2.43, but the operating system provides version 2.44` before every command. Confirmed across the estate, not just this app's database: `pg_database.datcollversion` is **2.43** for `projects`, `alfred` **and** `postgres`, while `ldd --version` reports glibc **2.44**
  - **Cause**: a routine glibc upgrade on a rolling-release distro. `libc`'s locale data changed the sort order of text, and PostgreSQL records the version a database was created with precisely so it can tell you it no longer matches
  - **Impact**: this is not cosmetic and it is not urgent, which is an awkward combination to hold. Any B-tree index on a text column was built against 2.43's ordering, so a lookup can miss a row that is present — an index scan walks to where the string *used to* sort. There are **25 indexes in the `sysadmin` schema** alone, several on text (`alerts.title` is matched with `ilike` in `resolve_alerts`, `service_health.service_name`). No wrong result has been observed here and glibc's changes between minor versions are usually confined to unusual scripts, so the realistic risk is low — but "low" is a probability, not a guarantee, and a silently missed row in `_active_alerts` would present as **an alert that never gets resolved or never gets deduplicated**, which is indistinguishable from the bugs this repository keeps finding by hand
  - **Found**: incidentally, by Session 39 running `psql` to check a column type. Nothing monitors it — and this application is the thing on this box whose job would be to
  - **Fix**: `REINDEX DATABASE projects` then `ALTER DATABASE projects REFRESH COLLATION VERSION`, per database, during a quiet window. **The `REFRESH` on its own is the trap**: it clears the warning by asserting the versions now match without rebuilding anything, so it converts a loud known-risk into a silent one. Deliberately not run in this session — it touches two other apps' data, and a reindex of a 16 GB database is not something to start while its owner is mid-session
  - **Worth its own follow-up**: a check for this belongs in the sysadmin agent. It is exactly the class of fault this service exists to catch — measurable in one query, invisible until something goes wrong, and currently discovered only when a human happens to open `psql`
  - **The check half landed 2026-08-13 (Session 44); the `REINDEX` half was carried out the same evening — by estate-manager, not here.** *(This bullet read "is still open and stays open" until 2026-08-16; the closure bullets are at the end of the entry, and the three days between are `SNAG-ESTATE-008`.)* `sysadmin/monitor/collation.py` reads both sides from `pg_database` on every sysadmin run and raises `Stale collation version on <db>` at `warning`, with `REINDEX` named before `REFRESH` in the message because REFRESH alone converts a loud known risk into a silent one. The remedy is deliberately not automated: a reindex of a 16 GB database touching two other apps' data wants a quiet window and a human, which is the same judgement the original entry took
  - **The count was wrong by more than half, and the shape of the error is worth keeping.** This entry says three databases (`projects`, `alfred`, `postgres`) because those are the three someone had opened a `psql` session against. `pg_database` is a **cluster-wide** catalog, so one query from the existing connection sees all eleven: **eight are stale** — the five above plus `alfred_dev`, `alfred_e2e`, `alfred_test`, `alfred_test_cc` and `template1`. Reading the catalog rather than the warnings is the difference, and it is the same lesson as SNAG-BRIEF-001: a symptom seen by hand samples wherever the hand happened to be
  - **`estate`, `estate_test` and `venture` are clean at 2.44**, having been created after the upgrade. That is the evidence that `CREATE DATABASE` stamps the *current* OS version rather than inheriting the template's — so `template1` sitting at 2.43 is not a trap for future databases, only for its own indexes. `template0` records no version at all, which is why the check requires **both** sides non-NULL and fails *open*: `recorded != actual` in Python reads NULL as a fault and invents an alert whose remedy does not exist. Deliberately the opposite of `schema_guard`, where not-knowing must fail closed
  - **In production since 2026-08-13 14:22**, and the numbers are the live ones: run 1 at 14:27:11 wrote `details->'collation' = {raised: 8, resolved: 0, mismatched: 8}` and opened eight `warning` rows; run 2 at 14:32:11 wrote `{raised: 0, mismatched: 8}` and the table still holds **8 collation rows in total**. Under the `_check_thresholds` pattern run 2 would have written eight more
  - **Verified live 2026-08-13** in a rolled-back transaction, both halves: run 1 raised exactly 8 rows for exactly the 8 stale databases; run 2 raised 0 with all 8 still open; narrowing the query as though two had been reindexed and one dropped resolved exactly those 3 and left 5 open. Residue after rollback: 0 rows
  - **Closed 2026-08-16, and not by this repository.** estate-manager carried out the remedy on 2026-08-13 in two passes, both committed there: `eb51ff6` at 18:03 took the safe set (`template1`, `postgres`, `alfred_dev`, `alfred_e2e`, `alfred_test`, `alfred_test_cc`) and `52b312c` at 20:29 took `alfred` then `projects`, rebuilt 20:23–20:24. The remedy is their `scripts/refresh-collations.sh`, which **never issues the `REFRESH` unless that database's `REINDEX` has just succeeded** and carries a comment telling the next reader not to simplify it by splitting the two — this entry's trap, encoded by the party that owns it. The estate owns it because it spans three applications' databases, which is the reason no single application had ever fixed it
  - **Verified here independently 2026-08-16, and the obvious method was not sufficient.** Index file mtimes show the two bursts (83 indexes in `alfred_test_cc` at 17:59, 206 in `projects` at 20:24) and prove nothing about an actively-written index, whose file carries a recent mtime whether or not its contents were rebuilt — a hole worth naming, because mtime is what a first pass reaches for. The exact test is `pg_class.relfilenode` against `pg_class.oid`: a rebuild draws a fresh relfilenode from the cluster-wide counter, so an index never rewritten since creation still has `relfilenode = oid`. Across all eight databases **0 of 125 collation-sensitive user indexes** retains its original relfilenode; `projects`' 58 sit in one band, 3,882,764–3,886,294, against creation OIDs from 46,010
  - **"Collation-sensitive" is the filter this entry lacked**, and it cuts the stated exposure down. The entry counts "25 indexes in the `sysadmin` schema, several on text". An index is only at risk where `pg_index.indcollation` holds something other than `0` (not collatable), `950` (`C`) or `951` (`POSIX`) — the last two being byte-order and therefore immune to a glibc change. Measured: **58** in `projects`, **125** across the cluster, **0** in `pg_catalog`
  - **The eight alerts resolved themselves at 18:01:48**, all together, on the sysadmin agent's next five-minute poll after the estate's first pass. This application observed the remedy land, correctly and within five minutes, and recorded it in the one place nothing reads back; `details->'collation'` has read `{"raised": 0, "resolved": 0, "mismatched": 0}` on every run since. That gap is filed as `SNAG-ESTATE-008`
  - **Two of this entry's numbers were true when written and are now wrong, in opposite directions.** "One of them 16 GB" was right: `projects` *was* 16 GB, the reindex reclaimed 2.8 GB of index bloat, and estate-manager's `09d231c` then dropped the retired `personal_assistant` schema on 2026-08-14, taking the database to 1,094 MB — so today's 1098 MB is PersonalAssistant's data being deleted, not this entry having been wrong. "Eight stale databases" was already this entry's own correction of "three", and the estate audits all eleven
  - **The quiet window this entry was nervous about was 30 seconds.** `REINDEX` rebuilds indexes, and `projects` held 3,123 MB of them inside its 16 GB — the size the entry cited was never the size that governed the work, which is the same shape as its own correction from three databases to eight: a figure taken from where the hand happened to be. estate-manager checked the preconditions this entry only implied — no session idle in transaction, 342 GB free, next timer 5.5 h away, superuser role
  - **`REINDEX DATABASE` skips system catalogues on PostgreSQL 14+**, so the paired `REFRESH` asserts conformance over indexes it never rebuilt — this entry's own trap one level down, and neither half of the estate's script covers it. Measured from both ends rather than reasoned about: estate-manager found 35 of `projects`' 4,162 indexes unrewritten, all catalogue TOAST indexes on `(chunk_id, chunk_seq)` with no collatable column, and **0 of the 1,531 carrying collation 100 skipped**; this repository arrived at the same answer from the other side, `pg_catalog` holding **0** collation-sensitive indexes at all because its text columns are `name`. No gap — but "no gap" is now a measurement, taken twice, independently

- [P3] SNAG-SYSD-003: `sysadmin.service` still orders itself after a service retired three weeks ago (2026-08-11)
  - **Symptom**: `After=network.target postgresql.service ollama.service`. Ollama was retired on 2026-07-24 when the LLM runtime migrated to llama.cpp (`alfred-inference.service`); there is no `ollama.service` on this box
  - **Impact**: none functionally — systemd ignores an `After=` on a unit that does not exist, and ordering never *requires*. The cost is documentary: the unit file is read as the record of what this service depends on, and it names the wrong inference runtime. Anyone deriving a new project's unit from it (which [monitorable-project.md](../guides/monitorable-project.md) invites) copies the stale dependency forward
  - **Found**: while editing the same file to add `StartLimitBurst` in Session 39
  - **Deliberately left**: the Session 39 edit needed to stay a single-purpose, reversible change with one rollback path. Removing a dependency line in the same commit as a restart-limit change means a rollback either reverts both or reverts neither. The correct successor is `alfred-inference.service`, and the honest question to settle first is whether this service should order against it at all — it does not need an LLM to start, and both weekly reviews already fall back to a deterministic digest when inference is unavailable, so the answer is probably to drop the line rather than replace it

- [P1] SNAG-BRIEF-001: `Project Health` publishes every project ever scanned, including retired ones (2026-08-07, **fixed 2026-08-11**)
  - **Fixed** in Session 36. Both project sections now render from **one** query and one filter (`status == "active"`), so the payload cannot give two answers; ordering is by score **ascending** because a cap over a descending sort shows exactly the rows carrying no information; the cap is `_BOARD_LIMIT` (5), shared with `Pick This Up`, and `facts.projects.omitted` reports what was cut. Live on 2026-08-11: **26 rows → 5**, and the two sections agree. The choice of `active` over `ACTIVELY_SCORED` was deliberate — the board and `/api/projects/next` both draw the line there, and the whole defect was two surfaces disagreeing
  - **Symptom**: The section carries **26 rows**, among them `PersonalAssistant`, `PersonalAssistant-auto` and `PA-worktrees` — a project retired 2026-07-24 whose repos are deliberately archived — plus four near-duplicate casings of the same work (`Portfolio` / `portfolio` / `portfolionew`, `BSL-Translator` / `bsl-translator` / `bsl-translation-app`). The same briefing's `Pick This Up` section lists 5 projects and `GET /api/projects/board` returns 6. One payload, three different answers to "what is on this box"
  - **Cause**: [`briefing._build_project_health_section`](../../sysadmin/services/briefing.py) selects the latest snapshot per project with **no status filter and no cap** — it never reads `findings["status"]`. `_build_next_actions_section`, 40 lines below it in the same file, does exactly that filter (`if findings.get("status", "active") != "active": continue`), and so does the board router. The health section predates the `status` declarations added to `projects.yaml` and was never brought forward
  - **Impact**: Directly undoes the highest-leverage line in [monitorable-project.md](../guides/monitorable-project.md) — declaring `status: dormant` is supposed to suppress board rows and staleness nagging at once, and it does everywhere *except* the one section a human actually reads each morning. A consumer cannot repair this: nothing in the section's rows carries status, so Alfred can only render 26 rows or filter by a hardcoded name list
  - **Fix**: Apply the same `status == "active"` filter, and cap the table the way `Pick This Up` is capped. Sorting by score already puts the 100s (all uninformative) first, so a cap without a re-sort would show the least useful rows — worth ranking by score *ascending* or by score-drop-since-last-scan while the filter is in
  - **Note**: 26 → 6 is a large enough drop that the section may stop justifying a table; the honest replacement is probably "3 below threshold" plus the offenders

- [P1] SNAG-BRIEF-002: `Pick This Up` truncates a next action mid-word with no marker (2026-08-07, **fixed 2026-08-11**)
  - **Fixed** in Session 36 with `truncate_at_word` in `sysadmin/core/text.py` — word boundary, and always the marker `… (truncated)`, matching what Alfred's own `sanitise_text` appends so a cut made here and a cut made there read identically. The 180 is now a **documented** cap (`NEXT_ACTION_CHARS`, written up in [alfred-briefing-integration.md](../guides/alfred-briefing-integration.md)) rather than an anonymous slice. Deliberately **not** capped at source in `roadmap.next_action_from_handoff`: the board serves the full field on purpose and the guide now says so, so moving the cap upstream would shorten a surface that had no defect to fix. The marker is the assertion the test makes, not the length — 180 characters of prose is indistinguishable from prose that happened to be 180 characters
  - **Symptom**: venture-assistant's row reads `"Next up → Review the first scoring sample from the 2026-08-06 nightly run: check \`SELECT lens, count(*), … FROM idea_score GROUP BY 1\` for"` — it stops mid-sentence and nothing says so. The board serves the same field at its full 293 characters, so **the same `next_action` has two lengths on two endpoints and the shorter one looks complete**
  - **Cause**: [`briefing._build_next_actions_section`](../../sysadmin/services/briefing.py) does `"next": action[:180]` — a bare slice, no ellipsis, no marker, no word boundary. The board applies no cap at all
  - **Impact**: A truncated action reads as a finished instruction, which is worse than a long one. Alfred's own contract (ADR-0063 §2) requires a visible `… (truncated)` marker and its `sanitise_text` adds one — but only to `text` sections; `table` cells pass through per-key untouched, so this producer-side cut is what reaches the page. A consumer cannot detect it: 180 characters of prose is indistinguishable from prose that happened to be 180 characters
  - **Fix**: Truncate at a word boundary and append a marker, or (better) don't truncate here — cap once at the source in [`roadmap.next_action_from_handoff`](../../sysadmin/services/roadmap.py) so board and briefing agree. The 180 that stays should be a *documented* cap, since [alfred-briefing-integration.md](../guides/alfred-briefing-integration.md) currently promises "one action per project" without saying how long one is

- [P2] SNAG-ROADMAP-001: An unfilled handoff placeholder is published as a real next action (2026-08-07)
  - **Symptom**: Alfred's `Pick This Up` row reads `"No unchecked task found — set one before the next session."` with `source: handoff` — i.e. a template's apology for having no next action is rendered as the next action, at the highest confidence level the honesty field has
  - **Cause**: Two compounding faults in [`roadmap.py`](../../sysadmin/services/roadmap.py). (1) `next_action_from_handoff`'s **first branch — the "next" heading path — never calls `is_placeholder` at all**; the detector is only wired into `first_unchecked_task` (lines 162, 166), which is the *fallback*. The deliberately-signalled path is the unguarded one. (2) Even adding the call there would not catch this line, because `_first_meaningful` strips leading/trailing underscores (`re.sub(r"^_+|_+$", "", line)`) **before** returning, and `_PLACEHOLDER_RES[0]` is `^_.*_$` — the strip removes precisely the marks the rule matches on. Verified: `is_placeholder("_No unchecked task found…_")` → `True`; `is_placeholder(_first_meaningful([same]))` → `False`
  - **Impact**: Narrower than it looks — `~/.claude/hooks/generate-handoff.sh` emits this italic line, so it will recur on every repo whose session ended without an unchecked task, and always with `source: handoff`. That is the value the guide tells consumers to "show plainly — this is the real thing", which is exactly backwards here. The right outcome is the row being **omitted**, which the builder already does for projects with no action
  - **Fix**: Run `is_placeholder` on the raw line before `_first_meaningful` normalises it (or have `_first_meaningful` return both raw and cleaned), and call it on the "next" heading path. Worth adding the generated line's own wording to `_PLACEHOLDER_RES` as a belt-and-braces rule, since it is emitted by a hook this box controls

- [P3] SNAG-SYSD-002: `kind: timer` recorded nothing, because the properties it reads were never requested (2026-08-08, fixed same day)
  - **Symptom**: Every `kind: timer` check returned `ok` with `last_run_recorded: false` and no `last_run`, `next_run` or `last_result`, for timers that had demonstrably fired — `alfred-evaluate.timer` last triggered 08:00 that morning. The check was not failing; it was recording nothing, and reporting success while doing so
  - **Cause**: [`_timer_facts`](../../sysadmin/monitor/agent.py) reads `LastTriggerUSec`, `NextElapseUSecRealtime` and `Result` out of whatever [`get_unit_status`](../../sysadmin/monitor/systemd.py) returns, and `get_unit_status` asks `systemctl show` for a **fixed property list** that contained none of them. The two modules agreed only by accident and had never agreed at all — the feature shipped inert in Phase 3
  - **Impact**: Narrow in effect, wide in lesson. Six timers were affected and none reported wrongly, they simply reported less than intended, so nothing looked broken. **The failure mode is the point**: a check that records nothing looks identical to a check that records nothing worth mentioning, which is the same shape as the dead `projects.yaml` paths this session exists to eliminate. It survived unit tests because those mock `get_unit_status` and supply the properties the assertions expect — the mock was the specification, and the real function had never been asked
  - **Fix**: Requested the three properties for every unit (systemctl omits inapplicable ones, so it costs nothing and avoids a second subprocess for the estate's six timers; `Result` is meaningful for services too, being how a oneshot reports its last outcome). Added a test asserting every property `_timer_facts` reads appears in `get_unit_status`'s request list — the coupling itself is now checked, rather than only the behaviour on a mock that satisfies it
  - **Found**: while installing `sysadmin-organiser.timer` and verifying it end to end rather than assuming, which is the step [monitorable-project.md](../guides/monitorable-project.md) requires and the only reason this surfaced

- [P2] SNAG-ROADMAP-002: `count_open_snags` miscounts a document that groups or cross-references its snags (2026-08-07)
  - **Symptom**: Two independent miscounts, both found while filing the twelve entries below. (1) A `###` sub-heading *inside* `## Open Issues` hid every snag under it — the count read 4 when 16 were open. (2) Once that was fixed the count read **21 for 16 snags**, because five nested `- **Cause**:` bullets happened to mention another snag's id
  - **Cause**: [`_sections`](../../sysadmin/services/roadmap.py#L79) splits on **any** heading level and returns a flat list, so a `###` under a `##` ends the parent section rather than nesting inside it; and `_SNAG_LINE_RE` is `^\s*[-*]\s+.*\bSNAG-[A-Z]+-\d+`, whose leading `\s*` makes an indented detail bullet indistinguishable from a top-level entry
  - **Impact**: `open_snags` reaches `GET /api/projects/board`, the briefing and `claude-preflight.sh`. Both directions are wrong in the dangerous way — grouping *hides* open bugs, cross-referencing *inflates* them — and neither is visible to the author, who sees a correct-looking document. This file is currently written around the parser rather than the parser matching the format
  - **Fix**: Nest sections by heading depth (a `###` under an "open" `##` inherits it), and require the id on an unindented bullet — or count entries by the `[P0]`/`[P1]`/`[P2]` marker, which only ever appears on a real entry line. Add a fixture covering both shapes: a grouped open section, and an entry whose sub-bullets name other snags
  - **Note**: the same `_sections` flattening governs `next_action_from_handoff` and `first_unchecked_task`, so a handoff using sub-headings under `## Next` is exposed to the first half of this

- [P2] SNAG-CFG-001: `notifications.desktop.min_severity` was read by nothing, and looked exactly like the knob that decides whether an alert speaks (2026-08-11, **fixed same day**)
  - **Symptom**: `config.yaml` carries `notifications.desktop: {enabled: true, min_severity: warning}` and `sysadmin/core/config.py` validates it through `DesktopNotificationsConfig`. **No module reads `config.notifications.desktop`** — verified by grep across `sysadmin/`, which finds the section's siblings (`.dnd` in `monitor/dnd.py`, `.pa_notify` and `.tray.mute_services` in `monitor/notifier.py` and `monitor/reliability_history.py`) and never `.desktop`. The setting that actually gates a desktop toast is **`tray.notify_min_severity`** in the separate top-level `tray:` section, parsed by `sysadmin_tray/config.py`, which the tray reads out of the same file
  - **Cause**: The backend used to own desktop notification and handed the job to the tray in Phase 3 (popup retired 2026-07-24). The producer moved; its configuration did not, and the tray brought its own key rather than adopting the existing one. Both currently read `warning`, so the two have never disagreed and nothing has ever surfaced the redundancy
  - **Impact**: Latent, and of the shape this repository keeps finding — a control that is *requested* but not *enforced*. Lowering `notifications.desktop.min_severity` to `info` changes nothing, and raising it to `critical` silences nothing; a reader tuning notification loudness edits the plausible knob and concludes the setting does not work. Found on 2026-08-11 while writing Session 31's idle nudges, whose whole ladder is designed around *which* severity is audible — three comments were written naming the wrong knob before the grep was run
  - **The defect was larger than the key.** `Notifier.send_notification` — the only method that had ever consulted this section — has **no production caller**: grep finds it in `tests/test_notifier.py` and nowhere else. `Notifier` is instantiated in `main.py`, started in the lifespan and attached to `app.state`, and nothing ever asks it to send an alert. `BaseAgent.raise_alert`'s own docstring says "the notifier service should be called separately" and `monitor/agent.py` says criticals are "to be picked up by notifier"; neither ever happened. The dead key was one symptom of a notification path that had been dead since it was written
  - **Fixed 2026-08-11** by giving the daemon a real notifier rather than deleting the config: `sysadmin/monitor/desktop.py` subscribes to `alert.raised` on the existing event bus and sends via `notify-send`. It is the tray's **understudy** — silent whenever the tray has polled `/api/sysadmin/alerts` within `tray_grace_seconds` — so the two can never both toast one alert, and the case it covers is the one that was previously silent: **the tray not running**, which was true on this box while the fix was being written
  - **The gate that makes it survivable is one-notification-per-incident.** The monitor writes one alert row per failed check, so a notifier that speaks per row is a denial of service against its own reader. Measured while building: 186 rows for one `venture-assistant` outage, 123 for one `internet` outage, 88 criticals/day at steady state, and **547,814 unresolved `Log error: kernel` rows** sitting in the table right now (SNAG-AGENT-002's damage). The daemon speaks only when no other alert with that title is already open, verified against those live rows: `Log error: kernel` → silent, an unseen title → speaks
  - **Subscribed, not called from `raise_alert`**, for two reasons: `core` must not import a domain (`test_import_boundary`), and `_queue_event` buffers until the run's transaction commits, so the "is another one open?" query cannot race the insert it is reacting to
  - **Both gates fail closed.** An unreachable database returns "not new" rather than "new", because the alternative turns a connection blip into a notification storm — which is the failure the module spends most of its code avoiding
  - **Still open, deliberately**: recovery is not announced. `alert.resolved` carries a *match pattern* rather than a subject (`"Project % health critical"`), so two of the three producers would publish something that reads as gibberish on a desktop. Announcing an outage's start and never its end is a real gap, recorded as one rather than papered over — see [tasks.md](tasks.md) Backlog

- [P2] SNAG-AGENT-002: Log aggregator raises one alert per error line (2026-07-24, **fixed 2026-08-12 by `SNAG-AGENT-005`; closed on paper 2026-08-17**)
  - **Symptom**: A single poll over a noisy unit produces dozens of separate alerts — observed live during Session 17 while streaming `/api/sysadmin/events`, where one log_aggregator run emitted alerts continuously
  - **Cause**: The aggregator raises an alert per matched error line rather than grouping by unit + error signature over the poll window
  - **Impact**: Partly masked downstream — Session 16's `NotificationPolicy` coalesces same-poll alerts into one toast and Session 17's SSE queue is bounded — so the user-visible noise is limited, but the `alerts` table still fills with near-duplicate rows and the alerts API/dashboard list is dominated by them
  - **Fix**: Group by unit + normalised message signature within a poll, raise one alert carrying an occurrence count (mirroring the "X flapped N×" pattern Session 16 used for notifications)
  - **Owned by [Session 27](tasks.md)** (promoted 2026-08-05) — the same signature fingerprinting this fix needs is also Tier 1 of the log-aggregator tiers, so the two are deliberately done together rather than the snag being patched twice
  - **Closed 2026-08-17 by Session 58 without touching any code, because it was fixed on 2026-08-12 and nobody closed the entry.** The "Fix" bullet above asks for grouping by unit plus a normalised message signature, one alert carrying an occurrence count. That is `sysadmin/monitor/log_signature.py` verbatim, shipped by `SNAG-AGENT-005` five days later against a table holding 598,091 rows — the two entries describe **one defect at two magnitudes**, and the later one measured it properly, so the earlier one had nothing left to ask for
  - **Measured 2026-08-17 rather than inferred from the commit**: the whole `alerts` table holds **8 unresolved rows**, five of them log signatures — one each for the two Bluetooth firmware messages, the hci reset, `alfred-backend.service` and `estate-broker-provision.service` — every one carrying `details['occurrences']` and `details['last_seen_at']`. The same query on 2026-08-11 returned 547,814 rows for `Log error: kernel` alone
  - **This is the `SNAG-ESTATE-008` shape for the third time**, with one difference worth recording rather than glossing: `STATUS.md`'s runners-up had **already noticed**, writing on 2026-08-16 that "`SNAG-AGENT-002`'s root cause went in Session 42 and the aggregator carries zero unresolved rows". The observation was made in the *ranking* and never carried back to the *entry* or to `tasks.md`, where the session still reads "take with SNAG-AGENT-002". So the failure here is not a missing measurement — it is a measurement that reached the document nobody acts from. The cost is that **Session 27 has been ranked as "fix the pile-up, then build the tiers"** when it is only the tiers, on an alerts table small enough to reason about for the first time in this application's life

- [P2] SNAG-TRAY-006: nothing tests that the 8400 producer still satisfies the tray's parse of the project routes (2026-08-13, **fixed 2026-08-14**)
  - **Handed over by the estate-manager session**, which found it during its project-state cutover and deliberately did not resolve it from outside — it is this repository's call. Recorded with the diagnosis corrected, because the way it was handed over is not the way it should be fixed
  - **What it looks like**: the tray now fetches `/api/projects/overview` and `/api/projects/{name}` from the estate's **8400** service, but parses the replies with *this* repository's `ProjectOverviewResponse`, `ProjectDetailResponse` and `ProjectHistoryPoint` — shapes the estate also defines on its own side. One shape, two repositories, which is the pattern estate-manager exists to remove
  - **Why "move them into estate-lib" is the wrong fix, and this is the part worth keeping**: the two are not duplicates, they are **different jobs**. The versions in [contracts.py](../../sysadmin/core/contracts.py) are a *consumer's tolerant parse* — `extra="ignore"`, every field defaulted, and a `ValidationError` meaning "connection lost" to the tray by design (the whole module docstring is about that). The estate's are a *producer's guarantee*, enforced as `response_model=`. Collapsing them makes the tray's defensiveness the producer's problem, and makes the producer's strictness a way for the tray to crash on an unknown field — the exact defect `extra="ignore"` was added to prevent
  - **What is actually missing is a test, not a class**: nothing anywhere asserts that what 8400 emits still parses through what the tray expects. A shared class would *appear* to provide that and would not — it would guarantee the two sides agree about field names while saying nothing about whether the producer is reachable, still serving those routes, or still populating them. The consumer-driven-contract shape is right: a test that fetches (or replays a recorded payload from) the live producer and runs it through `ProjectOverviewResponse.from_dict`
  - **Same shape as `SNAG-DB-003`, one layer up** — two hand-kept copies with nothing testing that they agree. Different resolution, because there the two copies genuinely should be one thing and here they should not
  - **Not urgent**: the tray fails safe. An unknown field is ignored and a missing one defaults, so producer drift degrades a tile rather than crashing the tray — which is also why this could go unnoticed for a long time, and is the argument for the test rather than against it
  - **Fixed 2026-08-14** by `tests/test_estate_project_contracts.py` (6 tests) with the two payloads recorded under `tests/fixtures/` — the first committed JSON fixtures in this repository, justified in the module docstring on the grounds that they are *another repository's output* rather than a payload the test author invented, and stamped with the date, box and exact curl commands that produced them
  - **The vacuity problem was the real design work**, and it is why "does it parse" was not the test. `extra="ignore"` plus universally-defaulted fields means `from_dict({})` **succeeds** — so the obvious assertion would have gone green against a producer that had stopped serving the routes' content entirely. Each half therefore checks the keys the consumer actually reads are present in the *raw* dict first, then that the parsed values are usable. `test_empty_payload_would_also_parse` pins the vacuity itself, so anyone tempted to simplify this module back to a bare `from_dict` can see what that would prove
  - **The asymmetry that made the detail route the valuable half**: `/api/projects/overview` carries `response_model=ProjectOverviewResponse` on the producer, but `/api/projects/{name}` has **no response model on either side** — estate-manager defines no `ProjectDetailResponse` or `ProjectHistoryPoint` at all, those having stayed with the monitor. Nothing anywhere enforced that route, which is exactly why it is the one that can drift
  - **A live divergence found by the diff, and deliberately not "corrected"**: `current["total_size_mb"]` arrives as an **int** on the detail route while every overview entry is a float. The column is `Integer`; the overview's `response_model=` coerces it and the unmodelled detail route lets the raw value through. The test asserts *numeric*, not `float` — asserting the symmetry would pin an accident of the producer's serialisation and fail the day a size is fractional, and `current` is `dict[str, Any]` so nothing on this side coerces it anyway
  - **The live half is gated on reachability, not on a marker or an env var**, following `tests/test_schema_drift.py`'s `_db_available()` shape; the skip was verified by pointing it at a dead port. Its URL is hard-coded rather than derived from `services.yaml`, for the reason `estate_judge`'s `base_url` is: a derived URL follows a rename silently and goes on passing against nothing. The live detail test takes its project name from the live overview, so a retired project cannot 404 the test for a reason the tray could never hit
  - **The tri-state is asserted rather than trusted.** `ProjectHistoryPoint.next_action_changed` is `bool | None`, so a producer sending `False` on the oldest point parses cleanly and silently invents a streak; the fixture test pins `[True, True, True, False, None]` explicitly, because the shape assertions alone never force a `False` to exist in committed data

- [P2] SNAG-DB-003: the autogenerate exclusion list is hand-copied across two files, and only one of them is tested (2026-08-13, **fixed 2026-08-16**)
  - **Filed by Session 43 at the request of the estate-manager session**, which hit it while cutting `sysadmin/projects/` over to the estate's 8400 service. Recorded here rather than there because it is this repository's code and this repository's test
  - **What is duplicated**: `include_object()` in [alembic/env.py](../../alembic/env.py) decides what autogenerate compares, and `_include_object()` in [tests/test_schema_drift.py](../../tests/test_schema_drift.py) is a hand-copy of it — the test's own docstring says "Mirror alembic/env.py". Both exclude non-`sysadmin` schemas and `alembic_version`; from 2026-08-13 both also exclude `FROZEN_TABLES` (`project_snapshots`, `project_reviews`), which are live tables whose models left with the package
  - **Why the copy is dangerous rather than merely untidy**: the two functions fail in *opposite directions*. The drift guard asserts an empty diff, so an exclusion present only in `env.py` makes the test fail loudly — annoying, and self-announcing. An exclusion present only in the **test** is silent: the guard goes green while the next `alembic revision --autogenerate` anyone runs still sees the tables as live-but-unmodelled and writes `op.drop_table('project_snapshots')` into a migration whose author was doing something else entirely. Green test, loaded gun, and the payload is the frozen history the drop task is deliberately deferring
  - **Already drifted once, in wording**: `env.py` carries `# Skip alembic's own version table` where the test carries a one-line docstring, which is the harmless version of exactly this
  - **Not fixed, and the fix is not obvious**: `env.py` cannot import from `tests/`, and a shared constant in `sysadmin/` would put a *testing* concern into the shipped package — arguably right, since `include_object` is production autogenerate configuration rather than test scaffolding, but it is a judgement call and Session 43 had no mandate to make it. Considered and rejected for now: a test asserting the two function bodies are textually identical (brittle against formatting, and it pins the copy rather than removing it)
  - **Interim mitigation is in place**: both functions carry the same comment naming the drop task, and they are kept textually identical apart from the leading underscore so that `diff` on them is meaningful
  - **Fixed 2026-08-16** by settling the placement question the entry left open: `sysadmin/metadata.py` owns `FROZEN_TABLES`, `include_object`, `include_name` and a `COMPARISON_OPTS` dict, and both callers splat it — `alembic/env.py` into `context.configure`, `tests/test_schema_drift.py` into `MigrationContext.configure(opts=…)`. The direction of ownership is what settles it: `include_object` is what `alembic revision --autogenerate` uses whether or not a test suite exists, so this is **production configuration the test borrows**, not test scaffolding pushed into the shipped package. It went beside `Base` rather than into a new module because that file's docstring already makes this exact argument for the *model set* — "a table missing from one copy and not the other is exactly the silent drift the drift test exists to catch" — and which tables the metadata is authoritative for is the same question one step further
  - **The fix was widened from the exclusion list to the whole comparison**, because the flags fail the same silent way. `compare_type`, `include_schemas` and `version_table_schema` were hand-copied too, and a flag set in `env.py` but absent from the guard leaves the guard green *while blind to the drift it certifies* — a weaker comparison passing itself off as the real one. Six option names now have exactly one statement between them
  - **What deliberately did not move**: the `SET search_path TO public` both callers issue. It belongs to the *connection* rather than to the comparison — `env.py` pairs it with `CREATE SCHEMA IF NOT EXISTS`, DDL the guard must never run — and its drift fails in the **loud** direction: a caller pointing search_path at `sysadmin` reflects every table twice and the guard fails with phantom diffs rather than passing while blind. Sharing it would have bought nothing and coupled a test to schema creation
  - **The copy is stopped from coming back by `tests/test_autogenerate_config.py`** (5 tests), an AST sweep over every module in the repository: no second `include_object`/`include_name`, no second `FROZEN_TABLES`, and none of the six option names passed by hand as a keyword or as an `opts` dict key. Textual because `alembic/env.py` **cannot be imported** — it runs the migrations at module scope. This is the opposite assertion to the one Session 43 rejected: not "the two bodies are identical" (which pins the copy) but "there is no second body"
  - **Two of the five tests exist because a detector that cannot fail proves nothing** — `SNAG-TRAY-006`'s vacuity lesson. One runs the walker at the owner, which must trip every rule (so a green sweep cannot mean the path was wrong), and one feeds it the code this session deleted. A third asserts both callers still *import* `COMPARISON_OPTS`, because a file that configures nothing at all would pass an absence check while silently taking alembic's defaults
  - **Verified live, not only against literals.** With the exclusion removed, autogenerate proposes `remove_index`/`remove_table` for both frozen tables against **3,739** and **4** live rows — the loaded gun, fired in a scratch script. After the change `uv run alembic check` reports "No new upgrade operations detected" (which exercises `env.py` itself, the file no test can import), `alembic upgrade head --sql` still renders offline mode, and re-adding a test-only exclusion to the drift guard makes the new sweep fail naming the file and line. Suite 1776 passed, ruff and mypy clean, **no migration**

**Project organiser — capability audit, 2026-08-07.** _Twelve defects found in
one pass over the project side and deliberately left unfixed so the audit
stayed an audit. Every one of them corrupts output Alfred already consumes, so
they are ordered before the structural work in [Session 35](tasks.md): building
a briefing envelope on top of wrong data only makes the wrong data better
formatted. Kept under this heading rather than a `###` sub-heading on purpose —
`roadmap._sections` splits on any heading level, so a sub-heading would hide
all twelve from `count_open_snags` and from preflight._

---

## Fixed Issues

_All SNAGs fixed to date are archived — nothing outstanding is hidden here._

| SNAG | Title | Fixed |
|---|---|---|
| SNAG-CI-001 | Cutover commit left `main` red on lint: the session ran `pytest` and took green for the gate, but CI runs `ruff check .` and `mypy` too — five `estate.registry` import blocks were unsorted | 2026-08-13 |
| SNAG-ROADMAP-003 | Four handoff conventions; scanner knew two and tuple order decided | 2026-08-10 |
| SNAG-PROJ-001 | Board freshness filter applied on one route out of nine | 2026-08-10 |
| SNAG-PROJ-002 | Deleted projects contributed to `average_active_score` | 2026-08-10 |
| SNAG-PROJ-003 | Project organiser never resolved its own alerts | 2026-08-10 |
| SNAG-PROJ-004 | 1,664 existing alert rows would never have cleared | 2026-08-10 |
| SNAG-PROJ-005 | Project review handed the model its figures | 2026-08-10 |
| SNAG-PROJ-006 | `/api/projects/stale` declared a `days` parameter it never read | 2026-08-10 |
| SNAG-PROJ-007 | `HACK`/`XXX` cost points and appeared in no column | 2026-08-10 |
| SNAG-PROJ-008 | TODO scan counted a project's own roadmap documents | 2026-08-10 |
| SNAG-PROJ-009 | `_count_todos`'s cap was per file, docstring said otherwise | 2026-08-10 |
| SNAG-PROJ-010 | `project_reviews` (and `disk_reviews`, `unit_audits`) never purged | 2026-08-10 |
| SNAG-PROJ-011 | Three places described `archived` as waiving git hygiene generally | 2026-08-10 |
| SNAG-PROJ-012 | Archived alert suppression absolute but documented as conditional | 2026-08-10 |
| SNAG-AGENT-005 | 598,091 log alert rows — one raised per log line, none resolvable | 2026-08-12 |
| SNAG-DB-001 | Un-applied migration caused a 39-hour monitoring blackout | 2026-08-10 |
| SNAG-CONF-001 | `sports_analyser` projects.yaml entry silently dead (wrong-case path) | 2026-08-04 |
| SNAG-SYSD-001 | `systemctl --user` checks always fail from the daemon (missing `XDG_RUNTIME_DIR`) | 2026-07-24 |
| SNAG-AGENT-003 | Shared `httpx.AsyncClient` reused across event loops | 2026-07-24 |
| SNAG-API-001 | Alert ack returned 200 with a malformed body for missing alerts | 2026-07-24 |
| SNAG-API-002 | Access-log middleware never excluded the real health endpoint | 2026-07-24 |
| SNAG-API-003 | Two endpoints blocked the event loop with sync psutil calls | 2026-07-24 |
| SNAG-TRAY-005 | Malformed API responses silently froze the tray on stale data | 2026-07-24 |
| SNAG-TRAY-004 | StatsPopup unreachable after dashboard cutover but still live | 2026-07-24 |
| SNAG-AGENT-001 | Project staleness scored from HEAD only, not all branches | 2026-07-24 |


### Session 37 write-up (handoff pipeline, fixed 2026-08-10)

**SNAG-ROADMAP-003 — four handoff conventions; the scanner knew two, and tuple order decided.**

- **Symptom**: `HANDOFF_PATHS` was `("docs/sessions/handoff.md", "docs/roadmap/handoff.md")`. The estate used **four** locations. `venture-assistant` kept a 6,044-byte `HANDOFF.md` at its root and `ImbaBots` a 141,096-byte `docs/handoff.md`; neither path was a candidate, so neither file was ever read. Both repos *also* carried a `docs/sessions/handoff.md` of 851 and 882 bytes — the SessionEnd hook's generated stub — and that was the file the scanner picked up. The next action for two active projects came from an 850-byte stub while a 6 KB and a 141 KB record sat unread beside it
- **Cause**: Two faults. (1) The candidate tuple was incomplete. (2) `_read` returned the first candidate that opened, making precedence a function of tuple order rather than of which document was current. `Alfred` had both listed candidates and happened to get the right one, by luck of ordering rather than by rule
- **Fix**: All four shapes listed, and `_read_handoff` selects by `handoff_date` — the document's own first-heading date, mtime as fallback — with tuple order breaking ties only. Also-rans are returned as `handoff_duplicates` rather than discarded, so a repo mid-migration is a reportable finding instead of a silent choice
- **The first attempt at the fix was wrong, and only the live estate showed it.** Ranking every *undated* candidate below every dated one is intuitive and re-creates the bug from the other side: `ImbaBots`' real handoff heads itself "Handoff — M5 (Tier 2)" with no ISO date, so the 882-byte stub written an hour *earlier the same day* outranked it purely for carrying one. The fallback has to apply uniformly — which is what `scan_roadmap` already did downstream when ageing a handoff, so the module had been holding two contradictory rules at once. The regression test uses real same-day timestamps deliberately: with bare epoch mtimes the stub wins honestly and the test would pass for the wrong reason
- **Root cause was upstream, and is also fixed.** The stubs existed because a SessionEnd hook wrote them. `SessionEnd` cannot block, so it could only emit what `git` already knew. It is retired in favour of a **Stop** hook that blocks until `HANDOFF.md` carries today's date — see [tasks.md](tasks.md) Session 37

### Session 34 write-ups (project-organiser capability audit, fixed 2026-08-10)

_Kept in full rather than summarised: each entry records what was measured
and why the chosen fix was chosen, which is the part that stops the defect
being reintroduced. Line references are to the pre-Session-35 layout
(`routers/projects.py`, `agents/project_organiser.py`); those modules are now
`projects/router.py` and `projects/agent.py`._

- [P1] SNAG-PROJ-001: the board's freshness filter is applied on one route out of eight (2026-08-07)
  - **Symptom**: A project whose directory has been deleted is still reported by `/overview`, `/stale`, `/report`, `/actions`, `/api/summary`, `_build_project_section` and `_build_next_actions_section`. `GET /api/projects/board` is the only surface that drops it
  - **Cause**: The board filters snapshots older than `newest_scan − 1h` ([`projects.py:400-416`](../../sysadmin/routers/projects.py#L400-L416)) *after* calling `_latest_snapshot_query`. Four of the other seven call sites do not use `_latest_snapshot_query` at all — they open-code the latest-per-name query, so there is no single place the filter could have been inherited from
  - **Impact**: Eight surfaces, at least two answers to "what is on this box". Same class as the `Project Health` defect above — a consumer cannot repair it, because nothing in the rows says the directory is gone
  - **Fix**: Move the cutoff **into the shared query** and route all eight call sites through it. Do not repeat the filter seven times — that is the shape the defect already has

- [P2] SNAG-PROJ-002: a deleted project still contributes to `average_active_score` (2026-08-07)
  - **Symptom**: The weekly project review's headline average includes projects that no longer exist on disk
  - **Cause**: `project_review.gather_review_data` selects the latest snapshot per name with no freshness filter — the previous entry's defect, at a call site outside the router
  - **Fix**: Same shared query. Listed separately because it is a separate call site in a separate module, and fixing the router alone would leave it

- [P1] SNAG-PROJ-003: the project organiser never resolves its own alerts (2026-08-07)
  - **Symptom**: **1,664 unresolved alert rows** live, 326 of them sharing one title. Every six-hourly scan re-raises for every project under threshold
  - **Cause**: `ProjectOrganiserAgent` never calls `BaseAgent.resolve_alerts`, though [`sysadmin_agent.py:268`](../../sysadmin/agents/sysadmin_agent.py#L268) and [`service_discovery.py:175`](../../sysadmin/agents/service_discovery.py#L175) both do
  - **Impact**: Unbounded, not merely noisy — retention purges **resolved** alerts only, so not one of these rows will ever expire. The alerts API and the tray's alert list are dominated by them
  - **Fix**: Call `resolve_alerts` on the recovery path, matching the title pattern the raise uses. The other two agents are the working reference

- [P2] SNAG-PROJ-004: the 1,664 existing rows will not clear themselves once resolution works (2026-08-07)
  - **Symptom**: Fixing the entry above resolves alerts raised *after* the fix. The backlog stays
  - **Cause**: `resolve_alerts` acts on the current scan's recovered projects; a historic row for a project that is currently still under threshold is not a recovery
  - **Fix**: Decide deliberately — a one-off backfill marking pre-fix rows resolved, or a dated migration, or leave them and let the retention purge take them once they are resolvable. **Do not ship the resolution fix without settling this**, or the table's size becomes permanent

- [P1] SNAG-PROJ-005: the project review hands the model its figures and asks it not to use them (2026-08-07)
  - **Symptom**: `build_review_prompt` for the project review passes scores, deltas, totals and recommendation point values into the prompt, relying on `REVIEW_INSTRUCTIONS` to tell the model not to restate them
  - **Cause**: The disk review was rebuilt figure-free by construction on 2026-08-06; the project review, which is the older of the two, was never brought into line
  - **Impact**: This exact approach is **recorded in CLAUDE.md as verified to fail**. Given "25.0 GB across 50 directories" plus an explicit "do not restate figures", dria-agent-a-3b restated them and published the quotient "each consuming 5GB". Instructing a model not to use a number it can see is a request; not showing it one is a constraint
  - **Fix**: Bands, phrases and directions in the prompt; every real figure in `build_facts_section`, prepended deterministically. Port the disk review's guard test — no digit reaches the model outside API paths

- [P2] SNAG-PROJ-006: `GET /api/projects/stale` declares a `days` parameter it never reads (2026-08-07)
  - **Symptom**: [`projects.py:92`](../../sysadmin/routers/projects.py#L92) declares `days: int = Query(default=30, le=365)`; the handler filters on `health_score < grade_bands.needs_attention_min` and never references `days`. The endpoint answers "which projects score badly", whatever the caller asks for
  - **Cause**: The parameter documents an intent the implementation dropped. It has no `response_model`, no contract-registry entry and no known consumer
  - **Fix**: Implement the parameter or delete the endpoint. Deletion is the honest default given no consumer exists — but check Alfred first, since a 404 is worse than a wrong answer

- [P2] SNAG-PROJ-007: `HACK` and `XXX` cost health-score points and appear in no column (2026-08-07)
  - **Symptom**: A project with 40 `HACK` markers is penalised for all 40; `todo_count` and `fixme_count` both read 0, so nothing on any surface explains the deduction
  - **Cause**: [`project_organiser.py:258-259`](../../sysadmin/agents/project_organiser.py#L258-L259) records `todos["TODO"]` and `todos["FIXME"]` only, while the penalty is `sum(todos.values())` over all four configured `todo_patterns`
  - **Fix**: Either store the full mapping (a `todos` JSON column beside the two counts) or penalise only what is recorded. Deductions on this service are attributable by design — an unexplained one breaks the rule the three scorers share

- [P2] SNAG-PROJ-008: the TODO scan counts a project's own roadmap documents (2026-08-07)
  - **Symptom**: `snag_list.md`, `tasks.md` and this very file count towards their own repository's TODO penalty. Writing up a snag lowers the score
  - **Cause**: The scan includes `*.md`, and the patterns carry no word boundary — so `TODOS`, `TODO_LIST` and a prose sentence containing "TODO" all match
  - **Fix**: Exclude `*.md` (or at least `docs/roadmap/`), and anchor the patterns with `\b`. The markers are a *code* signal; counting them in documentation inverts the incentive the score is supposed to create

- [P2] SNAG-PROJ-009: `_count_todos`'s cap is per file, and its docstring says otherwise (2026-08-07)
  - **Symptom**: [`project_organiser.py:354`](../../sysadmin/agents/project_organiser.py#L354) documents "Capped at 1000 matches"; `-m 1000` is grep's **per-file** limit, so a repo with 300 files can return 300,000
  - **Fix**: Fix the cap or the docstring — and prefer fixing the cap, since the number reaching the score is the one that matters. Note the interaction with the entry above: excluding `*.md` lowers the counts that make the cap load-bearing

- [P2] SNAG-PROJ-010: `project_reviews` is never purged (2026-08-07)
  - **Symptom**: Four rows today, growing one per week, forever
  - **Cause**: The table is in neither `retention_config` nor `TABLE_TIMESTAMP_MAP` in [`retention.py`](../../sysadmin/services/retention.py). `disk_reviews` (migration 005) should be checked at the same time — it was added by the same pattern
  - **Fix**: Add both to the map with a retention window chosen for *review* data, not check data. A weekly narrative is worth keeping far longer than 30 days of health checks

- [P2] SNAG-PROJ-011: three places document `archived` as waiving git hygiene generally (2026-08-07)
  - **Symptom**: `_analyse_project`'s docstring, [`projects.yaml:173`](../../projects.yaml#L173) and [`config.py:404`](../../sysadmin/config.py#L404) all describe archived projects as exempt from git hygiene. They are not: `archived` waives the **stale-branch deduction only**, and a `.git/index.lock` still costs an archived project 5 points
  - **Fix**: Correct all three to name the single deduction that is waived. Documentation-only — the behaviour is defensible, the description is not

- [P2] SNAG-PROJ-012: archived alert suppression is absolute but documented as conditional (2026-08-07)
  - **Symptom**: The docs read as though an archived project can still alert at a low enough score. It cannot, under any score
  - **Cause**: `_effective_threshold` returns `0` for archived, and the score is clamped with `max(0, …)`, so the alert condition is `score < 0` — unreachable by construction
  - **Fix**: Document it as absolute. Worth keeping the mechanism as-is: a threshold of 0 is a clearer expression of "never alert" than a special case, provided the guarantee is written down and tested


Full symptom/cause/fix write-ups:
[archive/completed_2026-08-05.md](archive/completed_2026-08-05.md) (2026-07-24 → 2026-08-04) ·
[archive/completed_2026-03-23.md](archive/completed_2026-03-23.md) (SNAG-TRAY-001/002/003)


---

## Creating New SNAGs

**Naming**: `SNAG-###` or use domain prefixes like `SNAG-WEB-###`, `SNAG-API-###`, `SNAG-DB-###`

**Priority levels:**
- `[P0]` - Critical: Blocks users or causes data loss
- `[P1]` - High: Important functionality broken
- `[P2]` - Medium: Workaround exists

**Format:**
```markdown
- [P1] SNAG-WEB-001: Brief description (YYYY-MM-DD)
  - **Symptom**: What the user sees
  - **Cause**: Root cause (if known)
  - **Fix**: What was done to fix it (when resolved)
```
